# Copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (defaults shown)
RELAY_MODEL=claude-opus-5
RELAY_DB_PATH=relay.db
RELAY_MAX_AGENT_STEPS=10
RELAY_MAX_RUN_COST_USD=0.50
