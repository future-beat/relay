---
phase: 05-run-event-persistence-live-feed
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/relay/events.py
  - tests/test_run_events.py
requirements: [DATA-03, DASH-01]
autonomous: true

must_haves:
  truths:
    - "A RunEventBroker fans a frame out to every subscriber's bounded queue without ever awaiting or raising"
    - "A full subscriber queue drops its OLDEST frame instead of blocking the caller — a slow or stalled dashboard viewer never delays or stalls a paid agent run (SC-4, observable)"
    - "project() builds each public frame field-by-field from a named allowlist and returns None to drop an event"
    - "A RunRecorder persists one run_events row per event through Database.transaction()"
    - "A write-tool event row and the tool's own write commit or roll back together (D-04); forcing the event insert to raise leaves the reply row absent"
  artifacts:
    - path: "src/relay/events.py"
      provides: "RunEventBroker (fan-out) + project() (allowlist redaction) + RunRecorder (persistence)"
      contains: "class RunEventBroker"
      min_lines: 90
    - path: "tests/test_run_events.py"
      provides: "drop-oldest, project-allowlist, and the load-bearing atomicity test"
      exports: ["test_publish_drops_oldest_and_never_blocks", "test_send_reply_and_its_event_row_commit_atomically"]
  key_links:
    - from: "src/relay/events.py:RunEventBroker.publish"
      to: "asyncio.Queue"
      via: "put_nowait, then get_nowait+put_nowait on QueueFull (drop-oldest, plain def)"
      pattern: "put_nowait"
    - from: "src/relay/events.py:RunRecorder.execute_and_record"
      to: "run_events"
      via: "one outer Database.transaction() wrapping tool exec + INSERT (write-tool event nests as a SAVEPOINT)"
      pattern: "transaction\\(\\)"
---

<objective>
Build the `events.py` module — the three contracts every wiring plan consumes: `RunEventBroker` (bounded, drop-oldest, fire-and-forget fan-out), `project()` (the SC-3 allowlist redaction), and `RunRecorder` (durable per-event persistence with the D-04 atomic-nesting seam). This is an interface-defining plan: agent.py (Plan 03) and main.py (Plans 03/04) import these directly.

Purpose: Isolate the two load-bearing behaviors — atomic write-tool persistence and fail-closed redaction — in the module that owns them, with mutation-checked tests, before any wiring can leak them.

Output: New `src/relay/events.py`; tests for drop-oldest, the project allowlist, and the load-bearing atomicity rollback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-run-event-persistence-live-feed/05-CONTEXT.md
@.planning/phases/05-run-event-persistence-live-feed/05-RESEARCH.md
@.planning/phases/05-run-event-persistence-live-feed/05-PATTERNS.md

<interfaces>
<!-- Contracts this module builds against. Use directly. -->

Database.transaction() (src/relay/db.py:156-204): nest-safe context manager; outermost commits/rolls
back the connection, inner levels open a SAVEPOINT. Verified 3-case proof in RESEARCH Pattern 2
(05-RESEARCH.md:201-224): outer{reply; nested{event}}+RAISE rolls BOTH back (case 2 = the D-04 guarantee).

RunRegistry (src/relay/runs.py:44-159): the sibling coordinator to copy for lifecycle discipline —
per-app-startup on app.state (never module-level, L53-64), idempotent removal (deregister L98-102 uses
pop(...,None)), snapshot() L108-110 for the D-14 initial frame. asyncio primitives bind to the loop
they are first used on (L60-64) — build the broker in lifespan, never at import.

AgentEvent (src/relay/models.py:38-46): `type: str`, `data: dict[str, Any]`. The event shapes project()
consumes come from agent.py yield sites:
  tool_use  (agent.py:301-303): {"tool", "input"}          -> project to `tool` name only, DROP input
  tool_result (agent.py:450-457): {"tool", "result", "is_error"} -> per-tool allowlist on result
  guardrail (agent.py:392-401 / 432-441): {"guard","tool","action",...} -> {guard,tool,action} only
  notice    (agent.py:414-425): {"kind","tool","retrieval_mode","cause","results"} -> safe as-is
  usage (agent.py:290) / resolution (agent.py:478-481) / error (agent.py:277,283-286): cost/steps/reason
  text  (agent.py:299): {"text"} -> DROP the prose entirely

tool executor return shapes (src/relay/tools.py):
  lookup_customer L34-45: {"found","customer":{name,email,plan,...},"recent_tickets":[...]}  -> DROP customer + recent_tickets
  search_docs L48-70: results[].{doc,heading,id,text,score}  -> {doc,id,score} only, NEVER text
  create_escalation L82: {"escalation_id","status"} ; send_reply L98: {"reply_id","status"} ; set_category L104: {"ticket_id","category"}  -> ids/enums, safe

Reference bodies (do not exceed these — copy closely):
  RunEventBroker: 05-RESEARCH.md:228-259 (Pattern 3)
  project() + per-tool table: 05-RESEARCH.md:295-334 (Pattern 5) + the Sensitive-Data Map :336-350
  execute_and_record: 05-RESEARCH.md:212-224 (Pattern 2)
  citation-allowlist analog to mirror: agent.py:150-172 ; "do not weaken" test convention: 05-PATTERNS.md:100,265
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunEventBroker — bounded, drop-oldest, fire-and-forget</name>
  <files>src/relay/events.py, tests/test_run_events.py</files>
  <read_first>
    - .planning/phases/05-run-event-persistence-live-feed/05-RESEARCH.md:228-259 (Pattern 3, the reference body) and :352-357 (Anti-Patterns: never async def publish)
    - src/relay/runs.py:44-110 (RunRegistry — the lifecycle/idempotency analog to mirror) and :1-19 (module docstring: on app.state, never module-level)
    - .planning/phases/05-run-event-persistence-live-feed/05-PATTERNS.md:33-65, 269 (broker assignment + drop-oldest is the one new primitive)
  </read_first>
  <behavior>
    - test_publish_drops_oldest_and_never_blocks (unit): construct RunEventBroker(maxsize=2) in the test (fresh per test, like RunRegistry); subscribe() one queue; publish 3 distinct frames; assert the queue holds exactly 2 frames, the OLDEST was dropped (the 2nd and 3rd survive), and publish returned normally each time (never awaited, never raised). Then publish into a broker with a subscriber whose queue is full AND whose get would also fail — assert publish still does not raise.
    - test_publish_is_synchronous (unit): assert `inspect.iscoroutinefunction(RunEventBroker.publish)` is False — publish must be a plain def so it can never suspend the paid run.
    - test_unsubscribe_is_idempotent (unit): unsubscribe a queue twice; the second call does not raise; _subs is empty.
    - MUTATION that must make drops-oldest red: change publish to `q.put_nowait(frame)` without the QueueFull drop-oldest branch — publish then raises QueueFull and the test fails.
  </behavior>
  <action>
    Create src/relay/events.py with a `RunEventBroker` class per RESEARCH Pattern 3: `__init__(self, *, maxsize=None)` defaulting maxsize from `settings.events_queue_maxsize` when None; `subscribe() -> asyncio.Queue` adds a new bounded queue to `self._subs` and returns it; `unsubscribe(q)` uses `self._subs.discard(q)` (idempotent, mirroring RunRegistry.deregister); `publish(frame: dict) -> None` is a PLAIN def that iterates subscribers and `put_nowait`s, and on `asyncio.QueueFull` does `get_nowait()` then `put_nowait()` (drop-oldest), swallowing QueueEmpty/QueueFull so it never raises into the run; and a `close()` that sets a closed flag and pushes a module-level `_CLOSE_SENTINEL` to every subscriber so open /events streams terminate (consumed in Plan 04). Do NOT build any instance at module import. Add the three behaviors to tests/test_run_events.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_run_events.py::test_publish_drops_oldest_and_never_blocks tests/test_run_events.py::test_publish_is_synchronous tests/test_run_events.py::test_unsubscribe_is_idempotent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def publish" src/relay/events.py` returns >=1 and `grep -c "async def publish" src/relay/events.py` returns 0 (publish is synchronous).
    - Source: `grep -c "put_nowait" src/relay/events.py` returns >=2 (initial put + re-put after drop).
    - Behavior: all three named tests pass; the drop-oldest test docstring names the no-drop-branch mutation.
    - No module-level broker instance: `grep -vE '^\s*#' src/relay/events.py | grep -c "= RunEventBroker(" ` returns 0.
  </acceptance_criteria>
  <done>RunEventBroker fans out with bounded drop-oldest queues, never awaits, never raises; idempotent unsubscribe; close sentinel present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: project() — the SC-3 allowlist redaction</name>
  <files>src/relay/events.py, tests/test_run_events.py</files>
  <read_first>
    - .planning/phases/05-run-event-persistence-live-feed/05-RESEARCH.md:295-350 (Pattern 5 body + per-tool table + Sensitive-Data Map)
    - src/relay/agent.py:150-172 (the citation allowlist — the fail-closed accept-set to mirror)
    - src/relay/tools.py:34-45, 48-70, 82, 98, 104 (executor return shapes that flow into tool_result.result)
  </read_first>
  <behavior>
    - test_project_tool_use_drops_input (unit): project(AgentEvent(type="tool_use", data={"tool":"lookup_customer","input":{"email":"ava@acmecorp.com"}})) returns exactly {"type":"tool_use","tool":"lookup_customer"} — no "input" key, no email anywhere in the dict.
    - test_project_lookup_customer_drops_customer (unit): project a tool_result whose result carries {"found":True,"customer":{"email":...,"name":...},"recent_tickets":[...]} returns only {type,tool,is_error} — no customer object, no recent_tickets.
    - test_project_search_docs_keeps_ids_not_text (unit): project a search_docs tool_result whose results carry {doc,heading,id,text,score} returns results entries with doc,id,score only and NO text key.
    - test_project_text_is_dropped (unit): project(type="text", data={"text":"...secret..."}) returns None or a frame with no "text" key (the prose is dropped).
    - test_project_never_spreads_raw_data (unit / the mutation guard): for a tool_use event whose input contains the sentinel string "LEAK_SENTINEL", assert "LEAK_SENTINEL" is not in json.dumps(project(event)). MUTATION that must make it red: add `**event.data` (or `**d`) to the tool_use frame — the sentinel then appears and the test fails. Name this mutation in the docstring.
  </behavior>
  <action>
    Add `project(event) -> dict | None` to events.py that builds every public frame field-by-field from a named allowlist per RESEARCH Pattern 5 — NEVER `{**event.data}`. Handle each type: usage/resolution (cost, steps, via), error (reason/status/type), tool_use (tool name only), guardrail ({guard,tool,action}), notice ({kind,tool,retrieval_mode,cause,results}), text (drop the prose), and tool_result via a `_project_tool_result(d)` helper that switches on `d["tool"]`: lookup_customer -> {type,tool,is_error} (drop customer+recent_tickets), search_docs -> {type,tool,results:[{doc,id,score}]} (never text), send_reply -> {type,tool,reply_id,status}, create_escalation -> {type,tool,escalation_id,status}, set_category -> {type,tool,category}, unknown tool -> {type,tool,is_error} (fail closed). Return None for anything unrecognised. Add the five behaviors to tests/test_run_events.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_run_events.py -k "project" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "event.data" src/relay/events.py` shows no `{**` spread — `grep -c "\*\*event.data\|\*\*d\b" src/relay/events.py` returns 0.
    - Behavior: all five project tests pass; the never-spreads test docstring names the `**d` mutation that flips it red.
    - project handles every AgentEvent type emitted by agent.py (tool_use, tool_result, text, guardrail, notice, usage, resolution, error) and returns None for unknown types (fail closed).
  </acceptance_criteria>
  <done>project() builds frames from an explicit allowlist, drops prose/inputs/customer data, and is mutation-guarded against raw spreads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RunRecorder + the load-bearing atomicity test</name>
  <files>src/relay/events.py, tests/test_run_events.py</files>
  <read_first>
    - .planning/phases/05-run-event-persistence-live-feed/05-RESEARCH.md:195-224 (Pattern 2 — recorder + execute_and_record body + the 3-case nesting proof)
    - src/relay/telemetry.py:57-78 (record_run — the single-INSERT-in-a-transaction shape to copy for the read/non-tool path)
    - src/relay/db.py:156-204 (transaction() nesting semantics) ; src/relay/tools.py:85-98 (send_reply's own transaction() that nests as a SAVEPOINT)
    - tests/test_lifecycle.py:496-538 (the closed-DB monkeypatch harness to mirror for the atomicity rollback)
  </read_first>
  <behavior>
    - test_a_recorder_writes_one_row_per_event (unit): build a RunRecorder(db, run_uid="u1", ticket_id=7); call record() for three AgentEvents; query run_events WHERE run_uid="u1" ORDER BY seq — assert 3 rows, seq = 1,2,3 (monotonic), each payload is the raw json.dumps(event.data).
    - test_send_reply_and_its_event_row_commit_atomically (integration, LOAD-BEARING): using a real file-backed Database with a seeded ticket, call recorder.execute_and_record(execute_bound=<a real send_reply bound executor>, ..., event_type="tool_result") but FIRST monkeypatch RunRecorder._insert_event to raise sqlite3.OperationalError mid-transaction. Assert BOTH: (1) the send_reply raised/propagated, (2) `SELECT COUNT(*) FROM replies` is 0 — the reply row rolled back with the failed event insert (case 2). Then run it again WITHOUT the monkeypatch and assert the reply row AND its run_events row are BOTH present (case 1). Re-open the DB from disk to prove durability (mirror test_lifecycle.py:227-234).
    - MUTATION that must make the atomicity test red: in execute_and_record, move the event INSERT OUT of the outer `with self.conn.transaction():` (persist it in a separate transaction after the tool commits) — the forced-raise case then leaves the reply row present (COUNT==1) and the test fails. Name this mutation in the docstring; this is the D-04 guarantee and must not ship vacuous.
  </behavior>
  <action>
    Add a `RunRecorder` class to events.py holding `(conn: Database, run_uid: str, ticket_id: int)` and a private `seq` counter. `_insert_event(self, type, data)` increments seq and INSERTs one run_events row (run_uid, ticket_id, seq, type, `json.dumps(data, default=str)`). `record(self, event)` opens its own single-INSERT `with self.conn.transaction():` and calls `_insert_event(event.type, event.data)` — used for read-tool and non-tool events where there is no sibling write to nest into. `execute_and_record(self, execute_bound, spec, name, raw_input, policy, *, event_type)` wraps ONE outer `with self.conn.transaction():` around `result, is_error = execute_bound(spec, name, raw_input, policy)` FOLLOWED BY `_insert_event(event_type, {"tool":name,"result":json.loads(result),"is_error":is_error})`, returning `(result, is_error)` — the tool's own transaction() nests as a SAVEPOINT so reply row + event row are atomic (D-04, RESEARCH Pattern 2). This whole unit runs inside asyncio.to_thread later (Plan 03); keep it synchronous. Add the two behaviors to tests/test_run_events.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_run_events.py::test_a_recorder_writes_one_row_per_event tests/test_run_events.py::test_send_reply_and_its_event_row_commit_atomically -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def execute_and_record" src/relay/events.py` returns >=1; the event INSERT sits inside the same `with self.conn.transaction():` block as the tool exec (single outer transaction).
    - Behavior: both named tests pass. The atomicity test ACTUALLY forces _insert_event to raise mid-transaction and asserts `COUNT(*) FROM replies == 0` — not merely that both rows exist on the happy path. Its docstring names the move-event-out-of-transaction mutation.
    - RunRecorder is synchronous (no async def); it never builds an asyncio primitive.
  </acceptance_criteria>
  <done>RunRecorder persists one row per event with monotonic seq; write-tool exec + event insert are atomic; the load-bearing rollback test forces the failure and is mutation-named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent step -> public projection | project() is the only path raw event.data may cross to become a public frame |
| paid run -> slow subscriber | publish must never let a stalled watcher backpressure the run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | project() allowlist | mitigate | Build every public field explicitly from a named allowlist; drop input/customer/text; unit-tested with a LEAK_SENTINEL mutation guard (test_project_never_spreads_raw_data). Full end-to-end leak test lands in Plan 03. |
| T-05-04 | Denial of Service | RunEventBroker.publish | mitigate | Plain (non-async) publish with bounded drop-oldest queues; test_publish_is_synchronous + test_publish_drops_oldest_and_never_blocks prove it cannot suspend or raise into the run |
| T-05-05 | Tampering / Repudiation | RunRecorder atomicity | mitigate | Write-tool event row shares the tool's transaction (D-04); the load-bearing rollback test forces _insert_event to raise and asserts the reply row is absent (case 2) |
| T-05-SC | Tampering | npm/pip installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_run_events.py -x -q` green
- `.venv/bin/python -m pytest -q` full suite >=288 passing
- `ruff check src tests` clean
- The two load-bearing tests (atomicity, project-never-spreads) each name their mutation in-docstring
</verification>

<success_criteria>
- events.py exports RunEventBroker, project(), RunRecorder with no module-level instances
- publish is synchronous, bounded, drop-oldest, non-raising
- project() is a fail-closed allowlist proven against a leak sentinel
- write-tool event persistence is atomic with the tool's write, proven by a forced-failure rollback test
</success_criteria>

<output>
Create `.planning/phases/05-run-event-persistence-live-feed/05-02-SUMMARY.md` when done
</output>
