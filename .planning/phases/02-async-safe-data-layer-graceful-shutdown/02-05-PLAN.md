---
phase: 02-async-safe-data-layer-graceful-shutdown
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - tests/test_lifecycle.py
  - fly.toml
  - Dockerfile
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Six tickets processed concurrently all stream normally and all land correct, fully-populated rows"
    - "No registered executor — agent registry or MCP registry — is a coroutine function"
    - "Tool execution provably runs off the event loop's thread"
    - "The registry is empty once a run completes, so an idle machine can still scale to zero"
    - "A stream cancelled before its generator starts registers nothing"
    - "A process request during drain is refused with 503"
    - "The three shutdown timeouts nest: fly.toml kill_timeout > uvicorn graceful window > settings.shutdown_drain_seconds"
    - "uvicorn is PID 1 in the container, so the platform signal reaches it directly"
  artifacts:
    - path: "tests/test_lifecycle.py"
      provides: "DATA-01-e/f/g, DATA-02-a/e, D-09, and the timeout-nesting invariant"
      contains: "test_overlapping_runs_all_record_without_locking_errors"
    - path: "fly.toml"
      provides: "top-level kill_timeout = 30"
      contains: "kill_timeout = 30"
    - path: "Dockerfile"
      provides: "exec-form CMD with --timeout-graceful-shutdown 20"
      contains: "--timeout-graceful-shutdown 20"
  key_links:
    - from: "tests/test_lifecycle.py"
      to: "relay.main.app.state.runs"
      via: "integration assertions against the live registry"
      pattern: "app\\.state\\.runs"
    - from: "fly.toml:kill_timeout"
      to: "Dockerfile --timeout-graceful-shutdown"
      via: "asserted nesting in test_shutdown_timeouts_nest_correctly"
      pattern: "kill_timeout"
    - from: "Dockerfile:CMD"
      to: "uvicorn as PID 1"
      via: "explicit exec inside sh -c"
      pattern: "exec uvicorn"
---

<objective>
Prove the phase. Append the integration and contract tests that only become meaningful once waves 1 and 2 have both landed (DATA-01-e/f/g, DATA-02-a/e, D-09), and set the two platform timeouts the drain is worthless without.

Purpose: the concurrency test is the one whose failure mode is flaky rather than deterministic — the naive wrapper failed 4 of 5 identical runs, and it failed with `customer_email=None` and spurious 404s, not with `database is locked`. And Fly's default 5s `kill_timeout` SIGKILLs the machine before the lifespan ever runs, which makes every line of drain code decorative.

Output: `tests/test_lifecycle.py` completed; `fly.toml` and `Dockerfile` carrying nested, asserted timeouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-CONTEXT.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-PATTERNS.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-VALIDATION.md
@tests/test_lifecycle.py
@tests/test_observability.py
@tests/helpers.py
@fly.toml
@Dockerfile

<interfaces>
<!-- Everything below already exists by wave 3. Consume it. -->

src/relay/runs.py:      RunRegistry.register(*, ticket_id) -> int / deregister(token) / .active / .snapshot() / async drain(*, timeout) -> bool / .draining
src/relay/main.py:      app.state.runs: RunRegistry;  async _get_ticket(id);  async process_ticket(ticket_id, dry_run=False) -> StreamingResponse
src/relay/tools.py:     build_registry(conn, kb_dir) -> dict[str, ToolSpec];  ToolSpec.execute: Callable[..., str]
src/relay/mcp_server.py: build_mcp_registry() -> dict[str, ToolSpec]      # READ ONLY — D-03 forbids editing this file
src/relay/config.py:    settings.shutdown_drain_seconds: float = 5.0
tests/helpers.py:       FakeClient(responses), TicketAwareFakeClient(), response/text_block/tool_use_block/usage
tests/conftest.py:      fixtures `conn` (:memory:), `db` (file-backed), `registry`, `client` (TestClient, owner key on default headers)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract and offload proof (DATA-01-e, DATA-01-f)</name>
  <files>tests/test_lifecycle.py</files>
  <behavior>
    - `test_no_registered_executor_is_a_coroutine_function`: build **both** registries — the agent's `build_registry(conn, KB_DIR)` and the MCP server's `build_mcp_registry()` — and assert `inspect.iscoroutinefunction(spec.execute)` is False for every entry in each. Assert both registries are non-empty first, so the test cannot pass vacuously.
    - `test_tool_execution_runs_off_the_event_loop`: drive one agent run whose only tool call is captured, and assert the executor observed a `threading.get_ident()` different from the event loop's thread id. Proves the offload actually happened, not merely that things still work.
  </behavior>
  <read_first>
    - `tests/test_lifecycle.py` (as created by plan 02-02 — append below its existing `# --- shutdown drain ---` section)
    - `src/relay/mcp_server.py` lines 100-158 — read only, to learn `build_mcp_registry`'s signature and what it needs; **do not edit this file** (D-03)
    - `src/relay/tools.py` and `src/relay/agent.py` — the `ToolSpec.execute` contract and the `asyncio.to_thread` seam being proved
    - `tests/conftest.py` — the `conn`, `registry`, and `client` fixtures and `KB_DIR`
    - `tests/test_observability.py` lines 1-40 — the fake-client injection pattern (`app.state.client = FakeClient([...])` after the fixture has started the app) and the module-local `_make_ticket` helper convention
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md` § "Pattern 2" and § user constraint D-02
  </read_first>
  <action>
Append a `# --- the sync executor contract ---` section to `tests/test_lifecycle.py` with the two tests named in `<behavior>`.

For `test_no_registered_executor_is_a_coroutine_function`, state in a comment why this test exists rather than what it does: the failure it guards is silent — an `async def` executor returns a coroutine object that is JSON-serialised into the model's context with no exception raised, and there is no mypy in CI to catch it. This test converts a silent corruption into a loud CI failure (D-02). Cover the MCP registry as well as the agent's, because `mcp_server.py` is frozen by D-03 and therefore cannot be adjusted if it drifts — the test has to be the thing that notices.

For `test_tool_execution_runs_off_the_event_loop`, build a `ToolSpec`-shaped entry (or wrap an existing one) whose `execute` records `threading.get_ident()` into a list, run it through the agent loop with a `FakeClient` scripted to make exactly one tool call then end the turn, and assert the recorded id differs from the id of the thread running the test coroutine. Do not assert on timing or sleep — thread identity is the deterministic signal.

Use the existing `conn`/`registry` fixtures where they fit; define a module-local `_make_ticket(client)` helper if one is needed rather than adding a shared fixture. Do not modify `tests/conftest.py` — plan 02-01 already appended the only fixture this phase adds.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_lifecycle.py -x</automated>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>.venv/bin/python -m ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_lifecycle.py::test_no_registered_executor_is_a_coroutine_function -x` passes.
    - `.venv/bin/python -m pytest tests/test_lifecycle.py::test_tool_execution_runs_off_the_event_loop -x` passes.
    - `grep -c 'build_mcp_registry' tests/test_lifecycle.py` is `>= 1` — the MCP registry is covered, not just the agent's.
    - `grep -c 'threading.get_ident' tests/test_lifecycle.py` is `>= 1`; `grep -c 'time.sleep' tests/test_lifecycle.py` is `0`.
    - Negative control: temporarily converting any `tools.py` executor to `async def` makes `test_no_registered_executor_is_a_coroutine_function` fail — confirm, then revert (leave the tree clean; `git status --porcelain src/relay/tools.py` must be empty afterwards).
    - The D-03 gate command prints **nothing**.
    - `.venv/bin/python -m pytest -q` reports **no failures**.
  </acceptance_criteria>
  <done>DATA-01-e and DATA-01-f are green, the coroutine-contract test provably fails when the contract is broken, and `mcp_server.py` is still unedited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Concurrency, registry lifecycle, and drain refusal (DATA-01-g, DATA-02-a, DATA-02-e, D-09)</name>
  <files>tests/test_lifecycle.py</files>
  <behavior>
    - `test_overlapping_runs_all_record_without_locking_errors`: six tickets processed concurrently through `process_ticket` with `TicketAwareFakeClient` produce **6 rows in `runs`, 6 rows in `replies` (one per distinct ticket id), and 6 tickets with status `resolved`**, and no error events. Assertions are on **row contents**, not on the absence of `OperationalError`.
    - `test_registry_is_empty_after_a_run_completes`: after one full run streams to completion, `app.state.runs.active == 0` and `snapshot() == []`.
    - `test_a_stream_that_never_starts_registers_nothing`: awaiting `process_ticket(id)` and never iterating the body leaves `app.state.runs.active == 0`.
    - `test_process_returns_503_while_draining`: with `app.state.runs.draining` set True, `POST /tickets/{id}/process` returns 503 with `detail["error"] == "shutting_down"` and a `Retry-After` header, and no spend reservation was claimed.
  </behavior>
  <read_first>
    - `tests/test_lifecycle.py` (current state — append below Task 1's section)
    - `tests/test_observability.py` lines 58-82 — the exact template for driving the handler coroutine directly (`stream = await process_ticket(id)`, `await stream.body_iterator.__anext__()`, `await stream.body_iterator.aclose()`), which `TestClient` cannot reach because it always drains the body
    - `tests/test_ratelimit.py` lines 392-399 and 423-433 — the 503 assertion shape and the `asyncio.run(process_ticket(ticket_id))` never-iterated pattern
    - `tests/helpers.py` — `TicketAwareFakeClient` from plan 02-03
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md` § "Pitfall 1" (the measured A/B and the exact malformed-row symptoms), § "Pitfall 4", and § "Pattern 4 → Refusing new work during drain"
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-VALIDATION.md` § "Critical note on DATA-01-d and DATA-01-g"
  </read_first>
  <action>
Append a `# --- concurrency and registry lifecycle ---` section with the four tests named in `<behavior>`.

`test_overlapping_runs_all_record_without_locking_errors` is the load-bearing test of the whole phase. Create six tickets through the `client` fixture, install `TicketAwareFakeClient` on `app.state.client`, then run all six `process_ticket` coroutines concurrently with `asyncio.gather`, fully draining each stream's `body_iterator`. Afterwards assert on **row contents**: six `runs` rows; six `replies` rows whose `ticket_id` set equals the six created ids; six tickets whose `status` is `resolved` and whose `customer_email` is a non-empty string. A test that only asserts "no exception raised" would have passed against the broken wrapper while the app fed the model a customer record with a null email — say that in the comment above the test so nobody weakens the assertions later.

`test_a_stream_that_never_starts_registers_nothing` must call the handler and never iterate the returned `body_iterator`. This is the CR-02 asymmetry: a `finally` in an async generator whose body never began does not run, so if registration were beside `reserve_run()` this test would see a leaked entry. Comment it with exactly that.

`test_registry_is_empty_after_a_run_completes` is the scale-to-zero guard (D-06). Comment it as such — "cheap to keep running" is a core-value constraint, and a leaked entry means the machine never reaches `stopped`.

`test_process_returns_503_while_draining` sets `app.state.runs.draining = True` directly (no need to actually shut down), posts through the `client` fixture, and asserts status 503, `response.json()["detail"]["error"] == "shutting_down"`, and the presence of `Retry-After`. Reset `draining` to False afterwards, or set it inside a `try/finally`, so the flag cannot leak into another test in the same `TestClient` context.

Do not touch `tests/conftest.py`, `tests/test_observability.py`, or any other existing test file (D-03).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_lifecycle.py -x</automated>
    <automated>for i in 1 2 3 4 5; do .venv/bin/python -m pytest -q tests/test_lifecycle.py::test_overlapping_runs_all_record_without_locking_errors -x || exit 1; done; echo "5/5 green"</automated>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>.venv/bin/python -m ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_lifecycle.py::test_overlapping_runs_all_record_without_locking_errors -x` passes on **5 consecutive invocations** — the loop command above prints `5/5 green`. A single green run proves nothing; the measured failure rate of the broken design was 4 in 5.
    - `.venv/bin/python -m pytest tests/test_lifecycle.py::test_registry_is_empty_after_a_run_completes -x` passes.
    - `.venv/bin/python -m pytest tests/test_lifecycle.py::test_a_stream_that_never_starts_registers_nothing -x` passes.
    - `.venv/bin/python -m pytest -q tests/test_lifecycle.py -k draining -x` passes and collects at least one test.
    - `grep -c 'customer_email' tests/test_lifecycle.py` is `>= 1` and `grep -c 'resolved' tests/test_lifecycle.py` is `>= 1` — the concurrency test asserts row contents, not just absence of exceptions.
    - `grep -c 'OperationalError' tests/test_lifecycle.py` is `0` **or** appears only alongside a content assertion — verify by reading; an exception-only assertion is a failed test design here.
    - `.venv/bin/python -m pytest -q` reports **>= 122 passed, 0 failed**.
    - The D-03 gate command prints **nothing**.
  </acceptance_criteria>
  <done>DATA-01-g, DATA-02-a, DATA-02-e and D-09 are green, and the concurrency test is stable across five consecutive runs.</done>
</task>

<task type="auto">
  <name>Task 3: Platform timeout arithmetic — fly.toml, Dockerfile, and the invariant that pins them</name>
  <files>fly.toml, Dockerfile, tests/test_lifecycle.py</files>
  <read_first>
    - `fly.toml` (in full — note that `kill_timeout` is a **top-level** key and TOML will parse it into the last table if it is placed after one; and the `[env]` block's comment voice)
    - `Dockerfile` (in full — the `CMD` line and the `#` rationale-above-each-line convention)
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md` § "Code Examples §6", § "Pattern 4", § "Runtime State Inventory", and § "Assumptions Log A1"
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-PATTERNS.md` § "`fly.toml`" and § "`Dockerfile`"
    - `src/relay/config.py` — `shutdown_drain_seconds`, the innermost of the three timeouts
  </read_first>
  <action>
In `fly.toml`, add the top-level key `kill_timeout = 30` after `primary_region` on line 7 and **before** `[build]` on line 9. Placement is load-bearing: TOML parses a bare key after a table header into that table. Comment it in the file's established voice — Fly's default is 5 seconds, which SIGKILLs the machine mid-run and skips the drain entirely; 30 covers a typical ~20s run plus lifespan teardown and stays under Fly's 300 maximum. **Do not add `kill_signal`** (D-08): Fly's own docs contradict themselves on the default, uvicorn handles SIGINT and SIGTERM identically, and pinning the wrong one is the only way to break this.

In the `Dockerfile`, change the `CMD` to the exec form inside `sh -c`: keep `sh -c` (it is needed for `${PORT:-8000}`) but prefix the command with `exec` so uvicorn replaces the shell and becomes PID 1, and append `--timeout-graceful-shutdown 20`. Comment both: `exec` removes any doubt that the platform signal reaches uvicorn rather than a shell that never forwards it; the flag exists because uvicorn's default is `None`, i.e. wait forever for an in-flight SSE stream — which Fly then SIGKILLs.

Append a `# --- shutdown timeout arithmetic ---` section to `tests/test_lifecycle.py` with `test_shutdown_timeouts_nest_correctly`, which parses `fly.toml` with `tomllib`, extracts `--timeout-graceful-shutdown <n>` from the `Dockerfile`'s CMD line with a regex, and asserts the strict nesting `kill_timeout > uvicorn graceful window > settings.shutdown_drain_seconds`. Also assert `exec uvicorn` is present in the CMD. Resolve both file paths relative to the test file's parent, the way `tests/conftest.py` resolves `KB_DIR`. Comment it with why it exists: three timeouts live in three different files and languages, and the only way this stays consistent through future edits is an assertion.

Record the two deploy-time checks that cannot run in CI — `fly config show` reporting `kill_timeout = 30`, and `fly machine list` still reaching `stopped` when idle — in the plan SUMMARY as deploy-time items, alongside Phase 1's two existing open deploy-time items. Do not attempt to run `flyctl`; it is not available here.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_lifecycle.py::test_shutdown_timeouts_nest_correctly -x</automated>
    <automated>.venv/bin/python -c "import tomllib; d=tomllib.load(open('fly.toml','rb')); assert d['kill_timeout']==30, d.get('kill_timeout'); assert 'kill_signal' not in d; print('fly ok')"</automated>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>.venv/bin/python -m ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import tomllib; d=tomllib.load(open('fly.toml','rb')); print(d['kill_timeout'], 'kill_signal' in d)"` prints `30 False` — top-level placement confirmed by the parser, and D-08 honoured.
    - `grep -c 'exec uvicorn' Dockerfile` is `1`; `grep -c -- '--timeout-graceful-shutdown 20' Dockerfile` is `1`.
    - `.venv/bin/python -m pytest tests/test_lifecycle.py::test_shutdown_timeouts_nest_correctly -x` passes.
    - Negative control: temporarily setting `kill_timeout = 3` in `fly.toml` makes that test fail; revert and confirm `git status --porcelain fly.toml` reflects only the intended change.
    - `.venv/bin/python -m pytest -q` reports **>= 123 passed, 0 failed**.
    - `.venv/bin/python -m ruff check src tests` prints `All checks passed!`.
  </acceptance_criteria>
  <done>`fly.toml` carries a top-level `kill_timeout = 30` and no `kill_signal`; the container CMD execs uvicorn with a 20s graceful window; and a test fails if any future edit breaks the nesting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform → container PID 1 | The shutdown signal must reach uvicorn itself, not a shell that swallows it |
| concurrent requests → shared connection | Six overlapping runs is the smallest configuration that exercises the hazard |
| MCP process → shared tool registry | The MCP entry point is frozen by D-03, so drift can only be detected by test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Information Disclosure | cross-request row leakage under concurrency | mitigate | `test_overlapping_runs_all_record_without_locking_errors` asserts row **contents** across six overlapping runs, five consecutive times. The measured symptom of the broken design was `customer_email=None`, so an exception-only assertion would not detect it |
| T-02-21 | Tampering | silent async contagion into `ToolSpec.execute` | mitigate | `test_no_registered_executor_is_a_coroutine_function` covers both the agent and MCP registries; the negative control proves it fails when the contract breaks (D-02) |
| T-02-22 | Repudiation | drain never runs because SIGKILL arrives first | mitigate | `kill_timeout = 30` > uvicorn `--timeout-graceful-shutdown 20` > `shutdown_drain_seconds = 5`, pinned by `test_shutdown_timeouts_nest_correctly` |
| T-02-23 | Repudiation | signal never reaches uvicorn (shell as PID 1) | mitigate | Explicit `exec` in the CMD removes assumption A1 entirely at zero cost; asserted by grep and by the nesting test |
| T-02-24 | Denial of Service (self-inflicted) | machine never scales to zero | mitigate | `test_registry_is_empty_after_a_run_completes` plus `test_a_stream_that_never_starts_registers_nothing`. Real-world counterpart (`fly machine list`) recorded as a deploy-time check |
| T-02-25 | Information Disclosure | drain refusal response | mitigate | Asserted to carry only `error` and `note` (ASVS V13) |
| T-02-SC | Tampering | dependency installs | accept | Phase installs no packages; `pyproject.toml` is not in `files_modified` |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → >= 123 passed, 0 failed
- `.venv/bin/python -m ruff check src tests` → `All checks passed!`
- Phase gate: `tests/test_lifecycle.py::test_overlapping_runs_all_record_without_locking_errors` green on 5 consecutive invocations
- `.venv/bin/python -m pytest tests/test_observability.py::test_mid_stream_disconnect_still_records_the_spend -x` → passes (DATA-02-f)
- `grep -v '^\s*#' src/relay/agent.py | grep -c 'async with'` → `0`
- `grep -v '^\s*#' src/relay/main.py | grep -c 'app.middleware\|BaseHTTPMiddleware'` → `0`
- D-03 gate: `git diff --name-only 8842c87 -- src/relay/mcp_server.py src/relay/evals.py tests/test_tools.py tests/test_mcp.py tests/test_guardrails.py tests/test_api.py tests/test_auth.py tests/test_ratelimit.py tests/test_observability.py tests/test_evals.py` prints nothing
- <human-check>Deploy-time only, recorded not executed: after `fly deploy`, `fly config show` reports `kill_timeout = 30`; a deploy during an active run shows `shutdown.drain_complete` in the logs rather than a truncated stream; `fly machine list` still reaches `stopped` when idle.</human-check>

## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Concurrent runs no longer block the event loop | 02-03 T2 (offload seam), 02-04 T1 (handler offload), 02-05 T1 (DATA-01-f) |
| GOAL | Concurrent runs no longer corrupt connection state | 02-01 T1 (materialised `Result` + `RLock`), 02-03 T1 (transactions), 02-05 T2 (DATA-01-g) |
| GOAL | Deploy restarts no longer lose run records | 02-02 T1 (`RunRegistry`), 02-04 T2 (drain before close), 02-05 T3 (timeout nesting) |
| REQ | DATA-01 | 02-01, 02-03, 02-04 T1, 02-05 T1/T2 |
| REQ | DATA-02 | 02-02, 02-04 T2/T3, 02-05 T2/T3 |
| ROADMAP SC-1 | Concurrent tickets stream normally, no thread errors, no stalls | 02-05 T2 (`test_overlapping_runs_all_record_without_locking_errors`, ×5) |
| ROADMAP SC-2 | Suite + MCP still work against the unchanged sync executor contract | 02-05 T1 (DATA-01-e, both registries); D-03 gate in every plan |
| ROADMAP SC-3 | SIGTERM mid-run lets the run finish before the DB closes | 02-04 T2 (drain ordering), 02-05 T3 (kill_timeout/graceful window) |
| ROADMAP SC-4 | An interrupted run still appears in `runs` with cost and outcome | 02-04 T2 (CR-01 `finally` preserved); gated by `test_mid_stream_disconnect_still_records_the_spend` (DATA-02-f) |
| RESEARCH | Pattern 1 materialised `Result` | 02-01 T1 |
| RESEARCH | Pattern 2 single `to_thread` seam | 02-03 T2 |
| RESEARCH | Pattern 3 `RunRegistry` | 02-02 T1 |
| RESEARCH | Pattern 4 three nested timeouts | 02-02 T2 + 02-05 T3 |
| RESEARCH | Pitfall 7 → recommendation (1)+(2): leave `enforce_daily_budget` on the loop, add the index | 02-01 T1 (index), 02-04 T1 (documented non-offload) |
| CONTEXT | D-01 single `to_thread` seam, sync `ToolSpec.execute` | 02-03 T2 |
| CONTEXT | D-02 mechanical coroutine-contract test | 02-05 T1 |
| CONTEXT | D-03 `mcp_server.py`/`evals.py`/existing tests untouched | D-03 gate command in every plan's verification |
| CONTEXT | D-04 hand-rolled registry, not `sse-starlette` | 02-02 T1 |
| CONTEXT | D-05 ~30s grace period + matching `kill_timeout` | 02-02 T2, 02-05 T3 |
| CONTEXT | D-06 registry empty on an idle server | 02-05 T2 |
| CONTEXT | D-07 preserve CR-01's `record_run`-in-`finally` | 02-04 T2 (preservation constraints + regression gate) |
| CONTEXT | D-08 do not set `kill_signal` | 02-05 T3 (asserted absent) |
| CONTEXT | D-09 503 while draining | 02-04 T3 (impl), 02-05 T2 (test) |
| CONTEXT | D-10 `idx_runs_created_at` | 02-01 T1 |
| CONTEXT | D-11 leave stale type hints, record deliberately | 02-01 output note |
| VALIDATION | DATA-01-a/b/c/d | 02-01 T2 |
| VALIDATION | DATA-01-e/f | 02-05 T1 |
| VALIDATION | DATA-01-g | 02-05 T2 |
| VALIDATION | DATA-02-a/e | 02-05 T2 |
| VALIDATION | DATA-02-b/c/d | 02-02 T3 |
| VALIDATION | DATA-02-f (existing regression) | 02-04 T2 verify + 02-05 verification |
| VALIDATION | D-09 row | 02-05 T2 |
| VALIDATION | Manual-only deploy-time rows | 02-05 T3 `<human-check>` + SUMMARY record |
| EXCLUDED | WR-01 TOCTOU | Deferred by `01-DEFERRED.md`; asserted untouched (`git diff` on `ratelimit.py` empty) |
| EXCLUDED | `run_events` / DATA-03 | Phase 5 |
| EXCLUDED | mypy in CI | Deferred Idea in CONTEXT.md |

No unplanned items.
</verification>

<success_criteria>
- All 14 rows of the VALIDATION.md verification map are green
- The concurrency test passes 5 consecutive times, asserting row contents
- `fly.toml` has top-level `kill_timeout = 30` and no `kill_signal`; the Dockerfile CMD execs uvicorn with a 20s graceful window
- A test fails if the three shutdown timeouts stop nesting
- Full suite >= 123 passing, ruff clean, and zero edits to `mcp_server.py`, `evals.py`, or any pre-existing test
</success_criteria>

<output>
Create `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-05-SUMMARY.md` when done. It must record: (a) the two deploy-time checks that cannot run in CI, appended to the project's open deploy-time list; (b) D-11's deliberate stale type hints in `mcp_server.py`/`evals.py`; (c) the optional CI improvement research recommends but this phase did not take — adding `docker stop --time=35 relay` plus an assertion on `shutdown.drain_complete` in `docker logs` to the CI `docker` job, the only automatable end-to-end check of the signal path.
</output>
