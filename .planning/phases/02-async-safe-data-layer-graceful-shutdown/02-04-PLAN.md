---
phase: 02-async-safe-data-layer-graceful-shutdown
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/relay/main.py
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "No HTTP handler executes SQLite on the event loop — every DB touch in main.py goes through asyncio.to_thread"
    - "Creating a ticket is one unit of work, and the created row is read back through the same offloaded path"
    - "A run registers itself as in-flight only once its stream actually starts, and always deregisters"
    - "Shutdown waits for in-flight runs before the database closes"
    - "A process request arriving during drain is refused with 503 rather than starting a paid run against a closing database"
    - "Phase 1's CR-01/CR-02 finally still records the run's cost exactly once and still releases the reservation"
  artifacts:
    - path: "src/relay/main.py"
      provides: "async _get_ticket, offloaded create_ticket//metrics, registry wiring, drain-before-close, 503-while-draining"
      contains: "app.state.runs"
  key_links:
    - from: "src/relay/main.py:lifespan"
      to: "app.state.runs.drain"
      via: "awaited before conn.close()"
      pattern: "await app\\.state\\.runs\\.drain\\([\\s\\S]*?\\)\\s*\\n\\s*conn\\.close\\(\\)"
    - from: "src/relay/main.py:event_stream"
      to: "app.state.runs.register"
      via: "first statement inside the generator body, never beside reserve_run()"
      pattern: "app\\.state\\.runs\\.register\\(ticket_id="
    - from: "src/relay/main.py:event_stream finally"
      to: "app.state.runs.deregister"
      via: "appended after release_run(token) in the existing plain finally"
      pattern: "release_run\\(token\\)\\s*\\n\\s*app\\.state\\.runs\\.deregister"
    - from: "src/relay/main.py:_get_ticket"
      to: "asyncio.to_thread"
      via: "the SELECT runs off the event loop"
      pattern: "await asyncio\\.to_thread"
---

<objective>
Wire the HTTP edge: offload every remaining event-loop SQLite call, make `create_ticket` a single transaction, instantiate the `RunRegistry` on `app.state`, drain it before `conn.close()`, register each run **inside** `event_stream`'s body, deregister after `release_run(token)`, and refuse new process requests with 503 while draining (D-09).

Purpose: this is the highest-risk file in the phase. It holds three Phase 1 fixes (CR-01's `record_run`-in-`finally`, CR-02's self-expiring reservation, CR-03's anon meter) that are preservation targets, and it is where uvicorn's "cancel but do not await" teardown races `conn.close()`.

Output: `src/relay/main.py` only. All new behaviour here is proved by plan 02-05's integration tests in wave 3; this plan's own gate is that nothing regresses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-CONTEXT.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-PATTERNS.md
@.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-VALIDATION.md
@.planning/phases/01-security-perimeter/01-DEFERRED.md
@src/relay/main.py
@src/relay/ratelimit.py

<interfaces>
<!-- Delivered by waves 1. Consume exactly these; do not re-derive or widen them. -->

From plan 02-01 — src/relay/db.py:
  class Database: execute(sql, params=()) -> Result   # Result: fetchone/fetchall/lastrowid
                  commit() / rollback() / close()
                  transaction()   # @contextmanager, yields the Database, commits on clean exit
  def connect(db_path) -> Database

From plan 02-02 — src/relay/runs.py:
  @dataclass(frozen=True) class ActiveRun: ticket_id: int; started_at: float
  class RunRegistry:
      draining: bool
      def register(self, *, ticket_id: int) -> int
      def deregister(self, token: int) -> None      # idempotent
      @property def active(self) -> int
      def snapshot(self) -> list[ActiveRun]
      async def drain(self, *, timeout: float) -> bool

From plan 02-02 — src/relay/config.py:
  settings.shutdown_drain_seconds: float = 5.0

Phase 1 contracts that must survive untouched (src/relay/ratelimit.py):
  def reserve_run() -> int
  def release_run(token: int | None) -> None
  def enforce_daily_budget(conn) -> None            # stays on the event loop by decision
  async def enforce(bucket, tier, request) -> None
  # 503 detail convention (ratelimit.py:208-222): HTTPException(503, detail={...}, headers={"Retry-After": ...})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offload every handler DB call and make ticket creation one transaction</name>
  <files>src/relay/main.py</files>
  <read_first>
    - `src/relay/main.py` (in full — `_get_ticket` at 245-251, `create_ticket` at 99-106, `get_ticket` at 109-111, `process_ticket` at 114-122, `/metrics` at 185-187, and the `_gate` docstring at 45-62 explaining why every control is a route dependency and never middleware)
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-PATTERNS.md` § "`src/relay/main.py`" — "Handler offload — the read path", "the write path", and "`/metrics`"
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md` § "Code Examples §2" — the verified handler shapes
    - `src/relay/db.py` (as rewritten by plan 02-01)
    - `tests/test_observability.py` lines 58-82 and `tests/test_ratelimit.py` lines 423-433 — the two existing tests that drive `process_ticket` directly, to confirm the `await`/`asyncio.run` call shapes still line up
  </read_first>
  <action>
Add `import asyncio` to the stdlib import group at the top of `src/relay/main.py`.

Turn `_get_ticket` into `async def _get_ticket(ticket_id: int) -> Ticket` whose SELECT runs inside `await asyncio.to_thread(...)` and whose `.fetchone()` is called inside the offloaded callable, not after it. Keep the `raise HTTPException(404, "ticket not found")` short-string form — short strings are for domain errors; the dict form is reserved for the perimeter's product-copy rejections. Add `await` at the three call sites: `create_ticket`, `get_ticket`, and `process_ticket`.

Rewrite `create_ticket`'s body per RESEARCH.md § Code Examples §2: an inner `def _insert() -> int` that opens `with app.state.conn.transaction() as db:`, executes the INSERT, and returns `cur.lastrowid` **from inside the block**; then `return await _get_ticket(await asyncio.to_thread(_insert))`. Drop the bare `conn.commit()` — `transaction()` commits on clean exit.

Change `/metrics` to `return await asyncio.to_thread(run_metrics, app.state.conn)`. `run_metrics` does `SELECT * FROM runs`, which is the one read that grows unbounded.

Deliberately **do not** offload `enforce_daily_budget(app.state.conn)` inside `_gate._dependency`. It is left on the loop by decision: contention is microseconds against a table of tens of rows, plan 02-01's `idx_runs_created_at` addresses the growth risk, and offloading would move a read of `ratelimit._reservations` — Phase 1 state this phase is told not to disturb — onto a worker thread. Record that choice in a comment at the call site so it reads as a decision, not an oversight.

Do not fix WR-01 (the TOCTOU between the budget check and the reservation). It lives in this exact code and is deferred to gap closure; leave the ordering of `enforce_daily_budget`, `enforce`, and `reserve_run` exactly as it is.

Do not introduce middleware. Every control stays a route dependency: a `StreamingResponse` locks its status line at 200 once the generator yields, so a rejection raised any later could only surface as an in-stream error on a 200.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_api.py tests/test_auth.py tests/test_ratelimit.py tests/test_observability.py -x</automated>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>.venv/bin/python -m ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import inspect; from relay.main import _get_ticket; assert inspect.iscoroutinefunction(_get_ticket); print('async ok')"` prints `async ok`.
    - `grep -c 'await asyncio.to_thread' src/relay/main.py` is `>= 3` (read path, insert, metrics).
    - `grep -c 'await _get_ticket(' src/relay/main.py` is `3`.
    - `grep -c 'with app.state.conn.transaction()' src/relay/main.py` is `1`.
    - `grep -v '^\s*#' src/relay/main.py | grep -c 'app.middleware\|BaseHTTPMiddleware'` is `0`.
    - `git diff 8842c87 -- src/relay/ratelimit.py` is empty — WR-01 untouched.
    - `.venv/bin/python -m pytest -q tests/test_api.py tests/test_auth.py tests/test_ratelimit.py tests/test_observability.py -x` passes with **zero edits** to any of them.
    - `.venv/bin/python -m pytest -q` reports **no failures** and a count `>= 110`.
    - `.venv/bin/python -m ruff check src tests` prints `All checks passed!`.
  </acceptance_criteria>
  <done>No SQLite statement in `main.py` executes on the event loop except the deliberately-retained daily-budget read; ticket creation is one transaction; the Phase 1 perimeter suite is green unedited.</done>
</task>

<task type="auto">
  <name>Task 2: Registry wiring — register inside the generator, drain before close</name>
  <files>src/relay/main.py</files>
  <read_first>
    - `src/relay/main.py` — `lifespan` at 22-32, the `reserve_run()` comment block at 126-132, `event_stream` at 134-153, and the `finally` at 154-180 **in full including its comments**; these comments state the invariants that must survive
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-PATTERNS.md` § "`src/relay/main.py`" — "Lifespan — the ordering that changes", "Registration inside the generator — the load-bearing detail", and "The `finally` that must not regress"
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md` § "Pitfall 4", § "Pitfall 5", § "Pitfall 6", and § "Code Examples §4 and §5"
    - `src/relay/runs.py` (as written by plan 02-02)
    - `src/relay/ratelimit.py` lines 37-49 — the reservation TTL comment, which documents the exact asymmetry that dictates where registration goes
  </read_first>
  <action>
Import `RunRegistry` from `.runs`; the import sorts between `.ratelimit` and `.telemetry` in the local relative group.

In `lifespan`, add `app.state.runs = RunRegistry()` alongside the other three `app.state` assignments. In the teardown after `yield`, add `await app.state.runs.drain(timeout=settings.shutdown_drain_seconds)` **immediately before** `conn.close()`. Comment it in this file's established voice — a *why*, at the exact line: uvicorn cancels in-flight request tasks when its graceful-shutdown window expires and then runs lifespan shutdown without awaiting those cancellations, so the stream's `finally` — which writes the run's cost that SEC-03's daily ceiling reads back — can still be pending here. Closing the connection first turns that into `Cannot operate on a closed database` and loses the row.

Make `run_token = app.state.runs.register(ticket_id=ticket.id)` the **first statement inside `event_stream`'s body**, before `started = time.perf_counter()` or immediately after it — but unambiguously inside the generator body and **not** beside `reserve_run()` in the handler. Comment it, mirroring the reasoning already written above `reserve_run()`: Starlette can cancel a `StreamingResponse` before its generator ever starts, and a `finally` in a generator whose body never ran does not execute. Registering outside the body would leak an entry on every aborted request, which stalls every future drain for the full grace period and leaves an idle server non-empty — breaking scale-to-zero (D-06). Registering inside the body makes register and deregister exactly balanced, which is why this needs no TTL where the reservation does.

Append `app.state.runs.deregister(run_token)` as the last statement of the existing `finally`, **after** `release_run(token)`.

Preservation constraints on that `finally` — all three are Phase 1 fixes with regression tests:
  - Keep it a plain `finally`, never a context manager. `run_ticket` suspends at every `yield`, and anything held across a `yield` leaks into whatever coroutine runs in between.
  - Keep the `recorded` guard and keep `record_run(...)` a **synchronous** call. Do not add an `await` to that branch — offloading it was measured to work but puts a new `await` in the most fragile fifteen lines in the app for no measurable benefit.
  - Keep `release_run(token)` where it is, after the row exists.
Do not reorder, rewrap, or reword the existing comments in that block; append to it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_observability.py::test_mid_stream_disconnect_still_records_the_spend -x</automated>
    <automated>.venv/bin/python -m pytest -q tests/test_lifecycle.py tests/test_db.py -x</automated>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>.venv/bin/python -m ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_observability.py::test_mid_stream_disconnect_still_records_the_spend -x` passes (D-07 / CR-01 regression gate — DATA-02-f).
    - `.venv/bin/python -m pytest tests/test_ratelimit.py -x` passes unedited (CR-02 reservation-TTL behaviour intact).
    - `grep -n 'app.state.runs = RunRegistry()' src/relay/main.py` matches inside `lifespan`.
    - `.venv/bin/python -c "import re,inspect; from relay import main; s=inspect.getsource(main.lifespan); i=s.index('drain('); j=s.index('conn.close()'); assert i<j, 'drain must precede conn.close()'; print('order ok')"` prints `order ok`.
    - `.venv/bin/python -c "import inspect; from relay import main; s=inspect.getsource(main.process_ticket); assert s.index('async def event_stream') < s.index('app.state.runs.register'), 'register must be inside the generator body'; assert 'reserve_run()' in s and s.index('reserve_run()') < s.index('async def event_stream'); print('placement ok')"` prints `placement ok`.
    - `.venv/bin/python -c "import inspect; from relay import main; s=inspect.getsource(main.process_ticket); assert s.index('release_run(token)') < s.index('app.state.runs.deregister'); assert 'if not recorded:' in s; print('finally ok')"` prints `finally ok`.
    - `grep -c 'await record_run\|asyncio.to_thread(record_run' src/relay/main.py` is `0`.
    - `grep -v '^\s*#' src/relay/main.py | grep -c 'async with'` is `0`.
    - `.venv/bin/python -m pytest -q` reports **no failures** and a count `>= 110`.
  </acceptance_criteria>
  <done>The registry is created per app startup, drained before the connection closes, registered inside the generator body and deregistered after `release_run` — with Phase 1's CR-01 and CR-02 behaviour provably intact.</done>
</task>

<task type="auto">
  <name>Task 3: Refuse new paid runs while draining (D-09)</name>
  <files>src/relay/main.py</files>
  <read_first>
    - `src/relay/main.py` — `process_ticket` at 114-133, particularly the ordering of `_get_ticket`, the 409 status check, and `reserve_run()`
    - `src/relay/ratelimit.py` lines 118-139 and 200-222 — the structured-detail 503 convention and the comment explaining why it diverges from the short-string form
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-PATTERNS.md` § "503-while-draining (D-09)" and § "Shared Patterns → Error responses"
    - `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-RESEARCH.md` § "Pattern 4 → Refusing new work during drain" and § "Open Questions #2"
    - `tests/test_ratelimit.py` lines 392-399 (`test_daily_budget_503`) — the assertion shape plan 02-05's test will copy
  </read_first>
  <action>
In `process_ticket`, immediately **before** `token = reserve_run()`, add a check on `app.state.runs.draining` that raises `HTTPException(503, detail={...}, headers={"Retry-After": ...})`.

Use the structured-detail convention from `ratelimit.py`, not the short-string form — a drain rejection is a perimeter rejection and its `detail` is read as product copy. The detail dict carries `"error": "shutting_down"` and a `"note"` explaining in plain language that the service is finishing in-flight runs before a restart and the caller should retry shortly. Send a small `Retry-After` header (a few seconds — this is a deploy, not an outage). Leak no internal state: no active-run counts, no ticket ids, no timeouts (ASVS V13).

Place it after the 409 already-processed check so an already-resolved ticket still gets its more specific error, and before `reserve_run()` so a refused request never claims spend it will not use. Comment it with what it prevents: a request arriving mid-drain would extend the shutdown window and can outlive `conn.close()`. Note in the same comment that this is best-effort — uvicorn stops accepting connections before the lifespan runs, so the window is narrow — which is exactly why it is one line and one test rather than a mechanism.

Do not add a drain check to `create_ticket` or `get_ticket`: those are cheap and non-paid, and widening the refusal surface is scope this phase did not buy.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_ratelimit.py tests/test_auth.py -x</automated>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>.venv/bin/python -m ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import inspect; from relay import main; s=inspect.getsource(main.process_ticket); assert s.index('draining') < s.index('reserve_run()'), 'drain check must precede reserve_run'; assert s.index('409') < s.index('draining'); print('order ok')"` prints `order ok`.
    - `grep -c 'shutting_down' src/relay/main.py` is `1`.
    - `grep -n 'Retry-After' src/relay/main.py` matches on the new 503.
    - The 503 detail leaks no internals: reading the dict, it contains only `error` and `note` keys — no counts, ticket ids, or timeout values.
    - `.venv/bin/python -m pytest -q tests/test_ratelimit.py -x` passes unedited — the existing `test_daily_budget_503` and `test_rate_limit_and_budget_ordering` still hold.
    - `.venv/bin/python -m pytest -q` reports **no failures** and a count `>= 110`.
    - `.venv/bin/python -m ruff check src tests` prints `All checks passed!`.
  </acceptance_criteria>
  <done>`POST /tickets/{id}/process` refuses with a 503 carrying `{"error": "shutting_down", ...}` and a `Retry-After` header once drain has started, without claiming a spend reservation. The behavioural test lands in plan 02-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → HTTP edge | Untrusted requests cross here; Phase 1's anon meter → auth → ceiling → tier window gate them |
| platform SIGTERM → lifespan teardown | Teardown ordering decides whether a run's spend row survives |
| streaming generator → registry | Registration is only balanced if it happens where the `finally` is guaranteed to run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Repudiation | `conn.close()` racing `event_stream`'s `finally` | mitigate | `await app.state.runs.drain(timeout=settings.shutdown_drain_seconds)` runs before `conn.close()`. A lost `runs.cost_usd` row is a SEC-03 spend-control bypass, not a cosmetic loss |
| T-02-15 | Denial of Service (self-inflicted) | leaked registry entry from a never-started stream | mitigate | `register()` is the first statement inside `event_stream`'s body, so register/deregister are exactly balanced. Same failure class as Phase 1's CR-02 leaked reservation, which was a remotely triggerable outage. Source gate asserts the ordering; behaviour gate is plan 02-05's `test_a_stream_that_never_starts_registers_nothing` |
| T-02-16 | Denial of Service | request arriving mid-drain extends shutdown past `conn.close()` | mitigate | D-09 503 on `app.state.runs.draining`, raised before `reserve_run()` |
| T-02-17 | Information Disclosure | 503 drain response body | mitigate | `detail` carries only `error` and product-copy `note`; no active counts, ticket ids, or timeouts (ASVS V13) |
| T-02-18 | Repudiation | double-writing or skipping the run row | mitigate | The `recorded` guard and the plain `finally` are preservation targets, gated by `test_mid_stream_disconnect_still_records_the_spend` (D-07) |
| T-02-19 | Elevation of Privilege | perimeter bypass via middleware conversion | mitigate | Every control stays a route dependency; grep gate keeps `app.middleware`/`BaseHTTPMiddleware` at 0 in `main.py`. A `StreamingResponse` locks its status at 200 once the generator yields |
| T-02-SC | Tampering | dependency installs | accept | Phase installs no packages; `pyproject.toml` is not in `files_modified` |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → no failures, count >= 110
- `.venv/bin/python -m ruff check src tests` → `All checks passed!`
- `.venv/bin/python -m pytest tests/test_observability.py::test_mid_stream_disconnect_still_records_the_spend -x` → passes (DATA-02-f)
- `grep -v '^\s*#' src/relay/main.py | grep -c 'app.middleware\|BaseHTTPMiddleware'` → `0`
- `git diff 8842c87 -- src/relay/ratelimit.py src/relay/auth.py` → empty (Phase 1 perimeter and WR-01 untouched)
- D-03 gate: `git diff --name-only 8842c87 -- src/relay/mcp_server.py src/relay/evals.py tests/test_tools.py tests/test_mcp.py tests/test_guardrails.py tests/test_api.py tests/test_auth.py tests/test_ratelimit.py tests/test_observability.py tests/test_evals.py` prints nothing
</verification>

<success_criteria>
- Every handler DB call is offloaded except the deliberately-retained `enforce_daily_budget` read, which carries a comment saying so
- `app.state.runs` exists per app startup; drain precedes `conn.close()`
- `register()` is inside the generator body; `deregister()` follows `release_run(token)`
- 503 with `{"error": "shutting_down"}` and `Retry-After` fires before `reserve_run()` while draining
- Phase 1's CR-01/CR-02/CR-03 behaviour is provably unchanged and WR-01 is untouched
</success_criteria>

<output>
Create `.planning/phases/02-async-safe-data-layer-graceful-shutdown/02-04-SUMMARY.md` when done. Note explicitly that the new behaviours (registry lifecycle, drain ordering, 503-while-draining) are gated by plan 02-05's tests in wave 3 — this plan's own gate is no-regression.
</output>
