---
phase: 03-semantic-retrieval
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/relay/tools.py
  - src/relay/guardrails.py
  - tests/test_tools.py
autonomous: true
requirements: [RAG-01, RAG-03, RAG-04]

must_haves:
  truths:
    - "search_docs delegates ranking to retrieval.retrieve and returns the same {\"results\": [...]} envelope, only ranking changed"
    - "each search_docs result carries {doc, heading, id, text, score}; envelope also carries retrieval_mode and degraded"
    - "build_registry calls load_index once and the search_docs closure captures it (no per-call disk read)"
    - "SendReplyInput.citations is optional (default_factory=list); an omitted citations argument still validates and executes"
    - "search_docs stays synchronous (no coroutine returned through ToolSpec.execute)"
  artifacts:
    - path: "src/relay/tools.py"
      provides: "search_docs semantic swap, send_reply citations param, build_registry load_index wiring, send_reply schema citations property"
      contains: "retrieval"
    - path: "src/relay/guardrails.py"
      provides: "SendReplyInput.citations optional field"
      contains: "citations"
    - path: "tests/test_tools.py"
      provides: "result-shape assertions + send_reply citations back-compat"
      contains: "citations"
  key_links:
    - from: "src/relay/tools.py search_docs"
      to: "src/relay/retrieval.py retrieve/load_index"
      via: "delegation; closure captures the loaded index"
      pattern: "retrieval\\.(retrieve|load_index)"
    - from: "src/relay/guardrails.py SendReplyInput"
      to: "send_reply executor"
      via: "optional citations list threaded through validate_tool_input"
      pattern: "citations.*Field\\(default_factory=list\\)"
---

<objective>
Swap `search_docs`'s keyword scorer for the hybrid retriever from `retrieval.py` while keeping the output envelope byte-compatible, and add the optional `citations` argument to `send_reply` (schema + input model + executor). Keep the sync `ToolSpec.execute` contract intact.

Purpose: This is the tool-surface change. Making `citations` OPTIONAL with a default (D-12) is the deliberate choice that keeps ~7 existing test files green — `[] ⊆ retrieved` always passes. The citation-guard enforcement itself lands in plan 03-05.
Output: modified `tools.py`, `guardrails.py`, `tests/test_tools.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-retrieval/03-RESEARCH.md
@.planning/phases/03-semantic-retrieval/03-PATTERNS.md
@.planning/phases/03-semantic-retrieval/03-CONTEXT.md

<interfaces>
<!-- retrieve() from plan 03-02 -->
From src/relay/retrieval.py:
- `load_index(kb_dir) -> Index`  (call once in build_registry)
- `retrieve(index, query, *, key, floor, max_results=3) -> (results: list[dict], mode: str, degraded: bool)`
  where each result = {doc, heading, id, text, score}

<!-- Current tool contract to preserve (RESEARCH: byte-compatible envelope) -->
From src/relay/tools.py:
- `@dataclass(frozen=True) class ToolSpec: schema; tier; input_model; execute: Callable[..., str]`
- `def search_docs(kb_dir, query, max_results=3) -> str` currently returns `json.dumps({"results": [...]})`
- `def send_reply(db, ticket_id, body) -> str`  (transaction() insert reply + resolve; return {reply_id, status})
- `build_registry(conn, kb_dir)` closures: search_docs → `lambda query: search_docs(kb_dir, query)`; send_reply → `lambda ticket_id, body: send_reply(conn, ticket_id, body)`; send_reply schema properties = {ticket_id:int, body:string}, required [ticket_id, body], additionalProperties False, strict True.

<!-- Current SendReplyInput -->
From src/relay/guardrails.py:
- `class SendReplyInput(BaseModel): ticket_id: int = Field(gt=0); body: str = Field(min_length=20, max_length=10_000)`

<!-- send_reply is called WITHOUT citations across ~7 files — must stay green (D-12) -->
tests/helpers.py TicketAwareFakeClient, tests/test_guardrails.py, test_lifecycle.py, test_observability.py, test_mcp.py, test_tools.py, test_db.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: search_docs semantic swap + build_registry load_index wiring</name>
  <read_first>
    - src/relay/tools.py (search_docs, send_reply, build_registry closures + schemas)
    - src/relay/retrieval.py (retrieve, load_index signatures)
    - .planning/phases/03-semantic-retrieval/03-PATTERNS.md § "src/relay/tools.py (MOD ...)"
    - .planning/phases/03-semantic-retrieval/03-CONTEXT.md D-01 (whole-file text, byte-compatible envelope)
  </read_first>
  <behavior>
    - search_docs returns `{"results": [...]}` unchanged in shape; results are now retrieval.retrieve output {doc, heading, id, text, score}; text is the WHOLE file (D-01).
    - envelope additively carries `retrieval_mode` ("semantic"|"keyword") and `degraded` (bool).
    - off-topic query → `{"results": [], "retrieval_mode": ..., "degraded": ...}` (floor produces empty; escalation signal preserved, D-03).
    - build_registry calls load_index(kb_dir) ONCE; the search_docs closure captures the loaded index (no per-call disk read).
    - "refund policy" still returns billing.md (keyword baseline holds with no key / no index).
  </behavior>
  <action>
    Change `search_docs` to accept the loaded index (via the captured closure) and delegate to `retrieval.retrieve(index, query, key=settings.voyage_api_key, floor=settings.retrieval_floor)`, then `json.dumps({"results": results, "retrieval_mode": mode, "degraded": degraded})`. Keep it a plain sync function returning `str` — do NOT make it async (would break the MCP sync path and the off-loop test). In `build_registry`, call `retrieval.load_index(kb_dir)` once at the top and have the search_docs closure capture that index (mirroring how conn/kb_dir are captured). Preserve the search_docs tool schema as-is (query only). Keep `max_results` behavior.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tools.py -k "search_docs" -x -q && grep -c 'async def search_docs\|async def send_reply' src/relay/tools.py</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `registry["search_docs"].execute(query="refund policy")` → parsed JSON has `results` with billing.md and keys {doc, heading, id, text, score}; envelope has `retrieval_mode` and `degraded`.
    - Source assertion: `grep` finds zero `async def search_docs`/`async def send_reply` (sync contract preserved).
    - Behavior: an off-topic query returns `results == []`.
  </acceptance_criteria>
  <done>search_docs returns the byte-compatible envelope with the new result shape via retrieval.retrieve; build_registry loads the index once; sync contract intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: send_reply optional citations + SendReplyInput + test_tools updates</name>
  <read_first>
    - src/relay/guardrails.py (SendReplyInput)
    - src/relay/tools.py (send_reply executor + schema + closure)
    - tests/test_tools.py (existing search_docs + send_reply tests)
    - .planning/phases/03-semantic-retrieval/03-CONTEXT.md D-12 and 03-RESEARCH.md Pitfall 2 (optional-with-subset-validation)
  </read_first>
  <behavior>
    - SendReplyInput gains `citations: list[str] = Field(default_factory=list)` — optional; omitting it validates as `[]`.
    - send_reply executor accepts a defaulted `citations=()` param so direct calls without citations still work; persistence behavior (insert reply, resolve ticket) unchanged.
    - send_reply tool schema gains an optional `citations` array property (NOT added to `required`); additionalProperties stays False.
    - existing citation-less scripted calls across the suite still pass (back-compat).
  </behavior>
  <action>
    Add `citations: list[str] = Field(default_factory=list)` to `SendReplyInput` in guardrails.py. Change the `send_reply` executor signature to `send_reply(db, ticket_id, body, citations=())` (persistence unchanged; the citations value is not written to the DB this phase). Update the send_reply closure in build_registry to `lambda ticket_id, body, citations=(): send_reply(conn, ticket_id, body, citations)`. Add an optional `citations` array-of-strings property to the send_reply `input_schema.properties` with a description instructing the model to cite ids returned by search_docs; leave `required` as [ticket_id, body]. In tests/test_tools.py, update the search_docs assertions to the new result shape (assert {doc, heading, id, text, score} keys and the envelope's retrieval_mode) and add a test that `send_reply.execute(ticket_id=..., body=...)` (no citations) still resolves, plus one where citations is supplied and validates.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tools.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `SendReplyInput().citations == []` (field optional).
    - Behavior: `registry["send_reply"].execute(ticket_id=<id>, body="...")` (no citations) returns status resolved.
    - Behavior: `validate_tool_input(SendReplyInput, {"ticket_id":1,"body":"x"*20})` succeeds with `citations == []`.
    - Test command: `pytest tests/test_tools.py -x` green.
    - D-12 back-compat, validated HERE not two waves later: `.venv/bin/python -m pytest -q` passes at `>= 150` with the send_reply signature/schema change in place, and `git diff --name-only <phase-base> -- tests/test_guardrails.py tests/test_lifecycle.py tests/test_observability.py tests/test_mcp.py tests/test_db.py tests/helpers.py` prints nothing — the ~7 existing send_reply call sites keep passing UNMODIFIED because `citations` defaults to empty and `[] ⊆ retrieved`.
  </acceptance_criteria>
  <done>citations is an optional validated list; send_reply executes with or without it; test_tools reflects the new search_docs shape and the back-compat send_reply calls; the full suite is green at this plan, not two waves later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → send_reply executor | model-supplied citations are untrusted (validated as list[str]; subset-checked in plan 03-05) |
| retrieval result → tool envelope | full-file text returned to the model; no secrets in the envelope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | model supplies malformed citations | mitigate | Pydantic `list[str]` validation on SendReplyInput (V5 input validation) |
| T-03-10 | Spoofing | citations argument shape drift breaks back-compat | mitigate | optional `default_factory=list`; `[] ⊆ retrieved` always passes (D-12) — keeps existing tests green |
| T-03-11 | Elevation of privilege | search_docs becoming async breaks the guarded seam | accept/mitigate | keep search_docs sync; grep gate asserts no `async def` — runs inside the existing to_thread seam |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_tools.py -x -q` green.
- `grep -c 'async def search_docs\|async def send_reply' src/relay/tools.py` == 0.
- `ruff check src tests` clean.
</verification>

<success_criteria>
search_docs delegates to the hybrid retriever and returns the byte-compatible envelope with the citation-shaped results; send_reply accepts an optional, validated citations list without breaking any existing scripted call.
</success_criteria>

<output>
Create `.planning/phases/03-semantic-retrieval/03-04-SUMMARY.md` when done.
</output>
