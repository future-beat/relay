---
phase: 03-semantic-retrieval
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/relay/retrieval.py
  - tests/test_retrieval.py
autonomous: true
requirements: [RAG-01, RAG-03, RAG-05]

must_haves:
  truths:
    - "A query embedding is ranked by cosine over the prebuilt matrix and results below the floor are dropped"
    - "An off-topic query (no keyword hit, no above-floor semantic hit) yields an empty result list"
    - "With no VOYAGE_API_KEY the retriever returns keyword results with mode='keyword' and no degradation flag"
    - "A Voyage failure returns keyword results with degraded=True (never raises)"
    - "Every result carries {doc, heading, id, text, score} with id == '{doc}#{slug(heading)}' and full-file text"
    - "load_index falls back to keyword-only mode on missing file, hash mismatch, or model/dim mismatch"
  artifacts:
    - path: "src/relay/retrieval.py"
      provides: "kb_sha256, heading parse/slug, load_index, cosine ranking, sync Voyage query, hybrid retrieve() with keyword fallback"
      min_lines: 80
      exports: ["kb_sha256", "load_index", "retrieve"]
    - path: "tests/test_retrieval.py"
      provides: "cosine/floor/empty/hybrid/degrade/keyword-baseline/citation-id-shape tests, all Voyage-free"
      contains: "def test_"
  key_links:
    - from: "src/relay/retrieval.py retrieve()"
      to: "voyage /v1/embeddings"
      via: "sync httpx.post with input_type='query'"
      pattern: "input_type.*query"
    - from: "src/relay/retrieval.py retrieve()"
      to: "keyword scorer"
      via: "union fallback when key unset or Voyage fails"
      pattern: "degraded"
---

<objective>
Create `src/relay/retrieval.py` — the whole-file embedding index loader, numpy cosine ranking with a similarity floor, a synchronous Voyage query call, the hybrid keyword+semantic union, and the `{doc, heading, id, text, score}` result shape (the join key for Phase 4/5/6). Cover it with Voyage-free unit tests.

Purpose: This module is the single place where retrieval logic lives; `tools.py` (plan 03-04) becomes a thin wrapper and `scripts/build_index.py` (plan 03-03) reuses the same `kb_sha256`/heading helpers so the builder and the CI staleness gate cannot drift.
Output: `retrieval.py`, `tests/test_retrieval.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-retrieval/03-RESEARCH.md
@.planning/phases/03-semantic-retrieval/03-PATTERNS.md
@.planning/phases/03-semantic-retrieval/03-CONTEXT.md

<interfaces>
<!-- Voyage request/response shape [VERIFIED vs docs, RESEARCH § Standard Stack] -->
POST https://api.voyageai.com/v1/embeddings
  Authorization: Bearer $VOYAGE_API_KEY
  json: {"input": [<str>...], "model": "voyage-4-lite", "input_type": "query"|"document", "output_dimension": 512}
  → {"data": [{"embedding": [<512 floats>], "index": 0}, ...], "usage": {...}}

<!-- Existing keyword scorer to reuse/move from tools.py (search_docs) -->
From src/relay/tools.py search_docs (current keyword logic to lift into retrieval.py):
- terms = re.findall(r"\w+", query.lower()) filtered to len>2
- per kb/*.md: score = sum(lower.count(term) for term in terms); keep score>0; sort desc

<!-- Config available (from plan 03-01) -->
From src/relay/config.py: settings.voyage_api_key, settings.voyage_model, settings.voyage_dim, settings.retrieval_floor

<!-- KB headings present (parsed at build time, stored in index.json) -->
kb/billing.md: "Billing and Plans", "Refunds", "Upgrades and downgrades"
kb/account.md: "Accounts and Access", "Password reset", "Two-factor authentication", "Data export and deletion", "SSO (Enterprise)"
kb/api.md:     "API Access", "Authentication", "Rate limits", "Webhooks"

<!-- index.json shape this module reads (produced by plan 03-03) -->
{"meta": {"model","output_dimension","input_type_document","kb_sha256"},
 "docs": [{"doc","headings":[...],"text":"<full file>","embedding":[...512...]}]}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build retrieval.py — kb_sha256, headings, load_index, cosine, sync Voyage query, hybrid retrieve()</name>
  <read_first>
    - src/relay/tools.py (search_docs keyword logic + the module docstring style to copy)
    - src/relay/agent.py lines 126-129 (the single sanctioned `# noqa: BLE001` narrow degrade-catch to mirror)
    - .planning/phases/03-semantic-retrieval/03-RESEARCH.md Code Examples 1, 2, 3 and § "Don't Hand-Roll" (numpy `mat @ q_norm`, `np.argsort`)
    - .planning/phases/03-semantic-retrieval/03-CONTEXT.md D-01/D-02/D-03/D-05/D-13
  </read_first>
  <behavior>
    - kb_sha256(kb_dir): hashlib.sha256 over `sorted(kb.glob("*.md"))` file bytes (index.json excluded since it is not *.md); builder and CI gate import THIS function.
    - headings(text): re.findall(r"^##?\s+(.*)$", multiline) → list of heading strings; slug(h) lowercases + hyphenates.
    - load_index(kb_dir): reads kb/index.json once, returns an in-memory object holding an L2-normalized (N,512) float32 matrix + parallel metadata (doc, headings, text). On missing file, kb_sha256 mismatch, or model/dim mismatch vs settings → return a keyword-only Index (matrix=None) and log a dotted warning; never raise (RAG-02 runtime degrades, RAG-05).
    - semantic query: sync httpx.post to Voyage with input_type="query", output_dimension=settings.voyage_dim, timeout ~10s + one manual retry; on any failure return None (caller marks degraded). Never raise into the run.
    - retrieve(index, query, *, key, floor, max_results=3) → (results, mode, degraded): keyword_hits = keyword scorer over the docs; if key set AND index.matrix is not None → attempt semantic (cosine = matrix @ normalize(q)), keep hits with score>=floor, mode="semantic"; on failure semantic_hits=[], mode="keyword", degraded=True; if no key/no matrix → mode="keyword", degraded=False. Union keyword + above-floor semantic (D-05); dedupe by doc.
    - result shape (D-06): each result {doc, heading, id, text, score} — text is the WHOLE file (D-01); heading = best-overlap ## section vs query (D-13), id = f"{doc}#{slug(heading)}"; deterministic fallback to first heading, or id=doc/heading=None when a doc has no ## headings.
    - empty path (D-03): when the union is empty, results == [] (the escalation signal).
  </behavior>
  <action>
    Create `src/relay/retrieval.py` with a module docstring in the existing "why, referencing the phase" style. Implement the helpers and functions per the behavior block. Keep every function SYNCHRONOUS — the blocking httpx.post runs inside the Phase-2 `asyncio.to_thread` seam at the call site (RESEARCH Pattern 2); do NOT introduce async. Use numpy for cosine (`mat @ q_norm`, `np.argsort`) — do not hand-roll dot loops. Catch Voyage failures with a single narrow `except Exception` annotated `# noqa: BLE001 — degrade, never end the run`, mirroring agent.py:128. Read the key from `settings.voyage_api_key` and pass it explicitly into the request headers; never log it. Store the query-dependent heading logic here so ids are anchored to real parsed headings.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_retrieval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `kb_sha256(kb)` is a 64-char hex string; re-running is stable.
    - Behavior: retrieve() with a monkeypatched Voyage embedding ranks the on-topic doc first and drops below-floor hits; off-topic query → results == [].
    - Behavior: with the Voyage call monkeypatched to raise, retrieve() returns keyword results and degraded is True; run never raises.
    - Source assertion: each result dict has exactly keys {doc, heading, id, text, score} and id matches `^[^#]+\.md#`.
  </acceptance_criteria>
  <done>retrieval.py implements the hybrid retriever, keyword fallback, degradation flag, and the citation-id result shape; all functions are sync; the module never raises a Voyage error into the caller.</done>
</task>

<task type="auto">
  <name>Task 2: test_retrieval.py — cosine/floor/empty/hybrid/degrade/keyword-baseline/citation-id, Voyage-free</name>
  <read_first>
    - tests/test_tools.py lines 15-23 (direct-executor + json-parse assertion style) and the no-match test
    - tests/conftest.py (KB_DIR, registry/conn fixtures, autouse _reset_limits)
    - .planning/phases/03-semantic-retrieval/03-VALIDATION.md § "Per-Task Verification Map" (RAG-01/03/05 rows) and § "Wave 0 Requirements"
    - .planning/phases/03-semantic-retrieval/03-PATTERNS.md § "tests/test_retrieval.py"
  </read_first>
  <action>
    Create `tests/test_retrieval.py`. Build a synthetic Index in-test (a small fabricated (N,512) matrix + metadata, or a temp index.json written to tmp_path) so NO test makes a real Voyage call. Cover, following the existing direct-call style: (1) cosine ranking puts the on-topic doc first when the query embedding is monkeypatched to a canned vector [RAG-01]; (2) a below-floor-only query returns `results == []` [RAG-03 empty, name it so `-k empty` selects it]; (3) hybrid union includes a keyword-only hit that semantic missed [D-05]; (4) `monkeypatch.setattr(settings, "voyage_api_key", None)` → mode=="keyword", degraded is False (the baseline, NOT a degradation) [RAG-05 baseline]; (5) Voyage call monkeypatched to raise → keyword results + degraded True, name it so `-k degrade` selects it [RAG-05]; (6) result carries {doc, heading, id, text, score} and `id == "{doc}#{slug}"`, name it so `-k citation_id` selects it [RAG-03]; (7) load_index on a missing/hash-mismatched file returns keyword-only mode without raising [RAG-02 runtime degrade]. Assert `input_type="query"` is what the query path sends (capture the httpx call kwargs) — a swap to "document" silently degrades recall (D-09). (8) **no-chunk structural guard [D-02]:** a returned result's `text` equals the full raw content of the corresponding `kb/*.md` file byte-for-byte — not a substring or chunk. This locks D-02 at the unit level so a future partial-chunking regression fails here cheaply, rather than only showing up as a degraded `grounded` score in the paid eval.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_retrieval.py -x -q && .venv/bin/python -m pytest -k "empty or degrade or citation_id" tests/test_retrieval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `pytest tests/test_retrieval.py -x` passes with VOYAGE_API_KEY unset (zero real Voyage calls).
    - Behavior: `-k empty`, `-k degrade`, `-k citation_id` each select at least one passing test.
    - Source assertion: a test captures the query request kwargs and asserts `input_type == "query"`.
    - Behavior (D-02 no-chunk): a result's `text` equals the full `kb/<doc>.md` file contents byte-for-byte, proving whole-file return rather than a chunk.
  </acceptance_criteria>
  <done>test_retrieval.py green with no key set; the RAG-01/03/05 behaviors and the input_type=query guard are all asserted deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → Voyage API | outbound HTTP with a bearer credential; response is untrusted JSON |
| KB files ↔ index.json | committed index must match current KB (checked via kb_sha256) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of service | Voyage outage/timeout ending runs | mitigate | 10s timeout + one retry; any failure → keyword fallback + degraded flag; retrieval.py never raises into the caller (RAG-05) |
| T-03-03 | Tampering | stale vectors after a KB edit | mitigate | load_index compares kb_sha256 and model/dim vs settings; mismatch → keyword-only mode + log (runtime half of RAG-02) |
| T-03-04 | Information disclosure | VOYAGE_API_KEY leakage | mitigate | key read from settings, passed only into request headers; never logged, never in `ctx` or query strings (RESEARCH V7) |
| T-03-05 | Tampering | malformed Voyage response (wrong dim/shape) | mitigate | narrow catch around parse; wrong shape → treated as failure → keyword fallback (RESEARCH Pitfall 6) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_retrieval.py -x -q` passes with no VOYAGE_API_KEY.
- `ruff check src tests` clean.
- `grep -c 'async ' src/relay/retrieval.py` returns 0 (module is fully synchronous).
</verification>

<success_criteria>
retrieval.py provides hybrid keyword+semantic retrieval with a floor, a keyword fallback with a degradation flag, and the `{doc, heading, id, text, score}` citation shape; every behavior is proven by Voyage-free unit tests.
</success_criteria>

<output>
Create `.planning/phases/03-semantic-retrieval/03-02-SUMMARY.md` when done.
</output>
