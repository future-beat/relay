---
phase: 03-semantic-retrieval
plan: 06
type: execute
wave: 5
depends_on: ["03-03", "03-05"]
files_modified:
  - src/relay/config.py
autonomous: false
requirements: [RAG-01, RAG-04, RAG-05]
user_setup:
  - service: voyage
    why: "The acceptance eval is the one activity that requires a real VOYAGE_API_KEY (live query embeddings)"
    env_vars:
      - name: VOYAGE_API_KEY
        source: "Voyage AI dashboard -> API Keys"
  - service: anthropic
    why: "The 12-case eval runs the real agent loop against Claude (approved paid spend, D-11)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys"

must_haves:
  truths:
    - "The retrieval_floor is calibrated so the off-topic case (salesforce-integration) returns [] while read-then-escalate cases are not starved"
    - "The 12-case eval pass rate is at or above the pre-change baseline, recorded as a per-case before/after diff"
    - "The chosen floor and the per-case diff are recorded in the SUMMARY as the phase's acceptance evidence"
  artifacts:
    - path: "src/relay/config.py"
      provides: "calibrated retrieval_floor value (replaces the plan 03-01 placeholder)"
      contains: "retrieval_floor"
    - path: ".planning/phases/03-semantic-retrieval/03-06-SUMMARY.md"
      provides: "chosen floor + before/after 12-case eval diff (acceptance evidence)"
      contains: "floor"
  key_links:
    - from: "src/relay/config.py retrieval_floor"
      to: "retrieval.retrieve floor gate"
      via: "the calibrated value gates semantic hits (D-03/D-04)"
      pattern: "retrieval_floor"
---

<objective>
Calibrate the similarity floor against the golden set and run the D-11 before/after 12-case eval diff — the primary acceptance gate for this phase. Record the chosen floor and the per-case diff as acceptance evidence.

Purpose: The floor value is a measured output, not a guess (D-04). Only the off-topic case (`salesforce-integration`) needs the floor to return `[]`; the three read-then-escalate cases (`refund-monthly`, `2fa-lockout`, `key-suspended`) escalate because the model reads doc text — the floor must NOT starve them. This activity is where the phase proves the retrieval swap did not regress the suite.
Output: calibrated `retrieval_floor` in `config.py`; acceptance evidence in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-retrieval/03-RESEARCH.md
@.planning/phases/03-semantic-retrieval/03-CONTEXT.md
@.planning/phases/03-semantic-retrieval/03-VALIDATION.md

<interfaces>
<!-- Eval harness (existing) -->
Run: `python -m relay.evals --concurrency 4 --threshold 0.8`
- 12 cases in evals/golden.jsonl; reports per-case action_ok / category_ok / grounded / pass; exits non-zero below --threshold.

<!-- The four escalation-expecting golden cases and why they escalate (RESEARCH Pattern 4) -->
- salesforce-integration → escalates because docs DO NOT cover it → floor MUST return [] for this query (the case the floor exists for)
- refund-monthly / 2fa-lockout / key-suspended → escalate because the model reads doc text saying a human is needed → the floor must NOT starve these (docs should still be retrieved)

<!-- retrieval_floor lives in config.py (placeholder set in plan 03-01) -->
settings.retrieval_floor: float
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Calibrate the floor + run the before/after 12-case eval diff (paid)</name>
  <read_first>
    - .planning/phases/03-semantic-retrieval/03-RESEARCH.md Pattern 4 (floor calibration nuance) and D-04/D-11
    - .planning/phases/03-semantic-retrieval/03-CONTEXT.md § "Specific Ideas" (floor calibration + eval diff are the same activity)
    - evals/golden.jsonl (the 12 cases)
  </read_first>
  <what-built>The full semantic-retrieval pipeline is in place (plans 03-01..03-05) with a placeholder floor. This step measures the correct floor and proves the eval suite does not regress — the required paid activity. It cannot be automated: it needs real VOYAGE_API_KEY + ANTHROPIC_API_KEY and approved Claude spend (D-11).</what-built>
  <how-to-verify>
    1. With both keys exported, capture the BEFORE baseline: run `python -m relay.evals --concurrency 4 --threshold 0.8` on the pre-Phase-3 tree (keyword retriever) — or use the recorded prior baseline if available — and note per-case pass/fail.
    2. On the current tree, run the eval at a few candidate floors. Confirm `salesforce-integration` returns `[]` (escalates) at the chosen floor, and that `refund-monthly`, `2fa-lockout`, `key-suspended` still retrieve their docs and reach their expected actions.
    3. Choose the floor that sits between salesforce's nearest-doc score (below floor) and the lowest genuinely-relevant match score (above floor).
    4. Confirm the AFTER pass rate is >= the BEFORE baseline and >= the 0.8 threshold.
  </how-to-verify>
  <acceptance_criteria>
    - CLI output: `python -m relay.evals --concurrency 4 --threshold 0.8` exits 0 (pass rate >= threshold) on the current tree at the chosen floor.
    - Behavior: at the chosen floor, salesforce-integration → create_escalation; the three read-then-escalate cases still reach create_escalation (not starved into errors).
    - Decision recorded: the chosen floor value and the per-case before/after diff.
  </acceptance_criteria>
  <resume-signal>Provide the chosen retrieval_floor value and the per-case before/after result table (or approve a proposed floor after reviewing the eval output).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Set the calibrated floor and record acceptance evidence</name>
  <read_first>
    - src/relay/config.py (retrieval_floor placeholder from plan 03-01)
    - the decision output from Task 1
  </read_first>
  <action>
    Set `settings.retrieval_floor` in `config.py` to the calibrated value from Task 1, updating the comment to note it was calibrated against the golden set (remove the "placeholder" note). Record in `03-06-SUMMARY.md`: the chosen floor, the per-case before/after 12-case eval table, and a one-line note that Voyage query cost is out-of-band and effectively $0 within the free tier and NOT tracked by RunBudget this phase (RESEARCH Pitfall 5 — a stated decision, not an accident). Re-run the fast suite to confirm no regressions from the config change.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q && ruff check src</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `settings.retrieval_floor` equals the calibrated value; the comment no longer says "placeholder".
    - Artifact: `03-06-SUMMARY.md` contains the chosen floor and the per-case before/after eval table.
    - Test command: full suite `.venv/bin/python -m pytest -q` green with VOYAGE_API_KEY unset.
  </acceptance_criteria>
  <done>The calibrated floor is committed; the acceptance evidence (floor + before/after diff + Voyage-cost decision) is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval process → Voyage + Claude | the one paid path in the phase; approved spend (D-11) |
| floor value → escalation behavior | a too-high floor starves valid matches; a too-low floor confidently returns off-topic docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering | miscalibrated floor breaks escalation preservation | mitigate | calibrate against the golden set; salesforce-integration must return [] while read-then-escalate cases keep their docs (D-04, verified by the eval) |
| T-03-17 | Repudiation | Voyage cost silently excluded from /metrics | accept | documented decision: embedding cost is out-of-band, effectively $0 in the free tier; not bolted into RunBudget this phase (RESEARCH Pitfall 5) |
| T-03-18 | Denial of service | paid eval overspend | mitigate | 12 cases, concurrency 4, one-time run; per-run RunBudget ceiling still applies to each case |
</threat_model>

<verification>
- `python -m relay.evals --concurrency 4 --threshold 0.8` exits 0 at the chosen floor (paid, checkpoint-gated).
- Full suite `.venv/bin/python -m pytest -q` green with VOYAGE_API_KEY unset.
- `03-06-SUMMARY.md` records the chosen floor and the before/after per-case diff.
</verification>

<success_criteria>
The floor is empirically calibrated so escalation is preserved for the off-topic case without starving read-then-escalate cases; the 12-case eval is at or above baseline and above threshold; the acceptance evidence is recorded.
</success_criteria>

<output>
Create `.planning/phases/03-semantic-retrieval/03-06-SUMMARY.md` when done, including the chosen floor and the before/after eval diff as the phase's acceptance evidence.
</output>
