---
phase: 03-semantic-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/relay/config.py
autonomous: false
requirements: [RAG-01, RAG-05]
user_setup:
  - service: voyage
    why: "Query-time and index-build embeddings via voyage-4-lite (RAG-01); unset key is the keyword-fallback baseline (RAG-05)"
    env_vars:
      - name: VOYAGE_API_KEY
        source: "Voyage AI dashboard -> API Keys (dashboard.voyageai.com). Local dev may leave it unset — keyword fallback is the intended no-key baseline."

must_haves:
  truths:
    - "numpy and httpx import successfully in the project venv"
    - "settings.voyage_api_key reads the un-prefixed VOYAGE_API_KEY (not RELAY_VOYAGE_API_KEY)"
    - "settings exposes voyage_model, voyage_dim, retrieval_floor with correct types"
  artifacts:
    - path: "src/relay/config.py"
      provides: "Voyage settings block (voyage_api_key alias, voyage_model, voyage_dim, retrieval_floor)"
      contains: "validation_alias=\"VOYAGE_API_KEY\""
    - path: "pyproject.toml"
      provides: "numpy + httpx runtime deps"
      contains: "numpy>=2.3,<3"
    - path: ".env.example"
      provides: "VOYAGE_API_KEY template line"
      contains: "VOYAGE_API_KEY"
  key_links:
    - from: "src/relay/config.py"
      to: "VOYAGE_API_KEY env var"
      via: "Field validation_alias (mirrors anthropic_api_key)"
      pattern: "voyage_api_key.*validation_alias=\"VOYAGE_API_KEY\""
---

<objective>
Add the numpy + httpx runtime dependencies and the Voyage configuration block that every later Phase 3 plan consumes. This is the foundation wave: no retrieval logic yet, only the dependency surface and typed settings.

Purpose: Later plans import numpy for cosine and httpx for the Voyage POST, and read `settings.voyage_*` for model/dim/floor/key. Getting the `validation_alias` right here prevents the silent `RELAY_` key-rename bug (RESEARCH Pitfall 4).
Output: Installed numpy/httpx, Voyage settings in `config.py`, `.env.example` line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-retrieval/03-RESEARCH.md
@.planning/phases/03-semantic-retrieval/03-PATTERNS.md

<interfaces>
<!-- Current config.py — extend, do not restructure. Copy the anthropic_api_key alias pattern exactly. -->
From src/relay/config.py:
- `class Settings(BaseSettings)` with `model_config = SettingsConfigDict(env_prefix="RELAY_", env_file=".env", extra="ignore")`
- `anthropic_api_key: str | None = Field(default=None, validation_alias="ANTHROPIC_API_KEY")`  ← the escape-hatch pattern to mirror
- Guardrails block: `max_run_cost_usd`, `price_in_per_mtok`, `price_out_per_mtok`
- Module exports a singleton: `settings = Settings()`

From pyproject.toml:
- `[project] dependencies` currently ends with `"limits>=5.8,<6",`
- `[project.optional-dependencies] dev` currently contains `"httpx>=0.27",`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify numpy and httpx package legitimacy before install</name>
  <read_first>
    - .planning/phases/03-semantic-retrieval/03-RESEARCH.md § "Package Legitimacy Audit" (both packages marked [ASSUMED]; slopcheck/pip were unavailable in the research environment)
  </read_first>
  <what-built>Nothing yet — this gate precedes the install. Both packages are flagged [ASSUMED] because slopcheck could not run during research. numpy is the canonical scientific-Python package; httpx is already transitive via anthropic (`httpx<1,>=0.25.0`).</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/numpy/ — confirm it is the real numpy (github.com/numpy/numpy), a 2.x line exists, and `>=2.3,<3` resolves on Python 3.12 (the CI target).
    2. Open https://pypi.org/project/httpx/ — confirm the real httpx (github.com/encode/httpx) and that `>=0.28,<1` exists.
    3. Confirm neither is a typosquat and both are PyPI (not npm) packages.
  </how-to-verify>
  <acceptance_criteria>
    - CLI output / human confirmation: both PyPI pages resolve to the canonical repos and the pinned ranges exist. Reviewer types "approved" or names the blocking issue.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add numpy/httpx deps and the Voyage settings block</name>
  <read_first>
    - src/relay/config.py (the whole file — Settings class + the anthropic_api_key alias at the guardrails-adjacent block)
    - pyproject.toml ([project] dependencies and [project.optional-dependencies] dev)
    - .planning/phases/03-semantic-retrieval/03-PATTERNS.md § "src/relay/config.py (MOD — Voyage settings)" and § "pyproject.toml (MOD — deps)"
  </read_first>
  <action>
    In `pyproject.toml [project] dependencies`, add `"numpy>=2.3,<3"` (a comment noting NOT >=2.5 — that requires Python 3.12+ and breaks the requires-python `>=3.11` floor) and add `"httpx>=0.28,<1"` promoting it from the dev extra to runtime (it is already transitive via anthropic). Leave or remove the `httpx>=0.27` line in the `[dev]` extra — a duplicate lower bound is harmless; promoting to runtime is the correct expression.

    In `src/relay/config.py`, add to the `Settings` class, grouped as a "Semantic retrieval (phase 3)" block: `voyage_api_key: str | None = Field(default=None, validation_alias="VOYAGE_API_KEY")` mirroring the existing `anthropic_api_key` escape hatch exactly (per RESEARCH Pitfall 4 — the RELAY_ prefix would otherwise silently rename the key); `voyage_model: str = "voyage-4-lite"` (D-09); `voyage_dim: int = 512` (D-09); and `retrieval_floor: float = 0.55` with a comment that this is a placeholder to be calibrated against the golden set in plan 03-06 (D-04). Import `Field` is already present.

    In `.env.example`, add a `VOYAGE_API_KEY=` line (un-prefixed, matching the validation_alias) next to the existing `ANTHROPIC_API_KEY=` line, with a short comment that leaving it empty selects the keyword-fallback baseline.

    Run `pip install -e ".[dev]"` in the project venv to install numpy/httpx.
  </action>
  <verify>
    <automated>python -c "import numpy, httpx" && python -c "from relay.config import Settings; s=Settings(); assert s.voyage_model=='voyage-4-lite' and s.voyage_dim==512 and isinstance(s.retrieval_floor,float)" && ruff check src</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Settings().voyage_model == "voyage-4-lite"`, `voyage_dim == 512`, `retrieval_floor` is a float.
    - Behavior: `VOYAGE_API_KEY=xyz python -c "from relay.config import Settings; assert Settings().voyage_api_key=='xyz'"` reads the un-prefixed name.
    - CLI output: `import numpy, httpx` succeeds; `ruff check src` is clean.
  </acceptance_criteria>
  <done>numpy/httpx installed and importable; Voyage settings present with correct defaults and the un-prefixed alias; `.env.example` documents the key; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build | pip resolves numpy/httpx from PyPI (supply-chain surface) |
| deploy env → process | VOYAGE_API_KEY is an outbound credential read from env/Fly secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | pip install numpy/httpx | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; both are top-tier PyPI packages, httpx already transitive via anthropic |
| T-03-01 | Information disclosure | VOYAGE_API_KEY | mitigate | Read via `validation_alias` from env/Fly secret; never a default, never logged (config only stores it; downstream plans must not put it in `ctx`/query strings) |
</threat_model>

<verification>
- `python -c "import numpy, httpx"` succeeds in the venv.
- `Settings()` exposes voyage_model/voyage_dim/retrieval_floor/voyage_api_key with the un-prefixed alias.
- `ruff check src` clean; existing suite still green (`.venv/bin/python -m pytest -q`).
</verification>

<success_criteria>
numpy and httpx are runtime dependencies and import cleanly; `config.py` carries the Voyage settings block with the correct alias and defaults; `.env.example` documents `VOYAGE_API_KEY`.
</success_criteria>

<output>
Create `.planning/phases/03-semantic-retrieval/03-01-SUMMARY.md` when done.
</output>
