---
phase: 04-evaluation-coverage
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/relay/agent.py
  - src/relay/evals.py
  - tests/test_evals.py
autonomous: true
requirements: [EVAL-03]

must_haves:
  truths:
    - "With the seeding hook armed, exactly one citation denial fires and a fake model recovers to a terminal send_reply"
    - "The seeding hook is keyword-only and default-off; it never alters production or the existing free suite"
    - "The paid dispatch can arm one report-only real-model recovery case via run_case"
  artifacts:
    - path: "src/relay/agent.py"
      provides: "keyword-only, default-off seed_citation_denial param on run_ticket"
      contains: "seed_citation_denial"
    - path: "src/relay/evals.py"
      provides: "run_case threads the arming flag to run_ticket (default off for the 12 golden cases)"
      contains: "seed_citation_denial"
    - path: "tests/test_evals.py"
      provides: "free mechanism test: hook drops a real id -> guard denies -> fake recovers"
      contains: "seed_denial"
  key_links:
    - from: "src/relay/agent.py::run_ticket (seed hook)"
      to: "src/relay/agent.py::_execute_guarded (citation guard, :150-172)"
      via: "reduced per-run retrieved_ids set forces one citation denial"
      pattern: "retrieved_ids"
    - from: "src/relay/evals.py::run_case"
      to: "src/relay/agent.py::run_ticket"
      via: "seed_citation_denial passthrough (default False)"
      pattern: "seed_citation_denial"
---

<objective>
Add the one genuinely new piece of machinery this phase needs: the D-08 denial-recovery seeding hook. A keyword-only, default-off `seed_citation_denial` param on `run_ticket` drops one real retrieved id and injects a dummy exactly once, forcing a single citation denial so a model can be observed recovering. Thread it through `run_case` for the paid dispatch (report-only), and prove the *mechanism* free with a recovering fake that reads valid ids out of the denial payload.

Purpose: Close the exact open item Phase 3 could not (03-REVIEW.md WR-10) — the citation guard never fired against a model, so in-run recovery was proven only against a scripted fake. This makes the guardrail claim measurable, per the phase goal.
Output: The opt-in hook (agent.py), the paid arming path (evals.py), and a free mutation-honest mechanism test (tests/test_evals.py). Production and the free suite are unaffected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evaluation-coverage/04-CONTEXT.md
@.planning/phases/04-evaluation-coverage/04-RESEARCH.md
@.planning/phases/04-evaluation-coverage/04-PATTERNS.md

<interfaces>
<!-- Verified self-analogs. The seed hook reuses fully-present machinery. -->

src/relay/agent.py:179-184 — current run_ticket signature (add `*, seed_citation_denial: bool = False`):
    async def run_ticket(client, registry, ticket, policy=None, budget=None) -> AsyncIterator[AgentEvent]

src/relay/agent.py:207 — retrieved_ids: set[str] = set()          # the per-run set (mutate ONLY this)
src/relay/agent.py:308-323 — the search_docs grow-step (amend once, when armed):
    if block.name == "search_docs" and not is_error:
        for hit in payload.get("results", []):
            retrieved_ids.add(hit["doc"]); retrieved_ids.add(hit["id"]); retrieved_ids.update(anchors...)
src/relay/agent.py:150-172 — the citation guard (denied_by="citation"; names retrieved_ids); DO NOT modify
src/relay/agent.py:57-73 — bind_to_ticket docstring: the codebase's stated aversion to forgettable per-call controls

src/relay/evals.py:182-234 — run_case(); the run_ticket call is at line 198:
    events = [e async for e in run_ticket(client, registry, ticket)]
src/relay/evals.py:302-304 — the 0.8 threshold gate; DO NOT touch

tests/test_guardrails.py:441-473 — RecoveringFakeClient (reads retrieved_ids out of the denial payload)
tests/test_guardrails.py:476-490 — test_run_recovers_after_citation_denial (structure to copy)
tests/test_guardrails.py:362-367 — keyword_baseline fixture
tests/test_guardrails.py:381-385 — _events_of helper
tests/test_guardrails.py:214-226 — _seed_tickets / _reply_ticket_ids
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opt-in, default-off seed_citation_denial hook on run_ticket</name>
  <files>src/relay/agent.py</files>
  <read_first>
    - src/relay/agent.py:179-212 — run_ticket signature + retrieved_ids creation
    - src/relay/agent.py:305-323 — the search_docs grow-step to amend
    - src/relay/agent.py:150-172 — the citation guard this exercises (do not modify)
    - src/relay/agent.py:57-73 — bind_to_ticket aversion-to-footguns rationale
    - 04-PATTERNS.md:220-271 — the recommended hook shape + isolation checks
    - 04-RESEARCH.md:228-245 — the seeding-hook sketch; Pitfall 4 (no registry mutation)
  </read_first>
  <behavior>
    - Armed + model cites the dropped id: the citation guard denies exactly once (guard="citation").
    - Default (unarmed): behavior byte-identical to today — all 256+ existing tests stay green.
    - Only the per-run `retrieved_ids` set is mutated; `app.state.registry` is never touched.
  </behavior>
  <action>
    Add a keyword-only, default-off param `seed_citation_denial: bool = False` to `run_ticket` (after `budget`, behind `*`). Track a local `_armed = False`. Inside the search_docs grow-step (agent.py:308-323), once, guarded by `if seed_citation_denial and not _armed and payload.get("results"):` — after the normal grow, drop one real id the model will likely cite (`dropped = results[0]["id"]; retrieved_ids.discard(dropped)`) and add a dummy (`retrieved_ids.add("__seeded_missing__")`) so the set stays non-empty; set `_armed = True`.

    Mutate ONLY the per-run `retrieved_ids` set (Pitfall 4 — never `app.state.registry`). Do NOT modify the citation guard (agent.py:150-172) or `bind_to_ticket`. Do NOT wire this into `main.py` (production must never arm it). The denial the guard emits already names the reduced `retrieved_ids` (agent.py:171), so it stays recoverable by construction.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import inspect,relay.agent as a; p=inspect.signature(a.run_ticket).parameters['seed_citation_denial']; assert p.default is False and p.kind.name=='KEYWORD_ONLY'" && ! grep -q seed_citation_denial src/relay/main.py && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>run_ticket has a keyword-only seed_citation_denial=False param; only the per-run set is mutated; main.py never references it; full free suite still >= 256 passing (no existing test changed).</done>
  <acceptance_criteria>
    - `inspect.signature(run_ticket)` shows `seed_citation_denial` is KEYWORD_ONLY with default `False`.
    - `grep -q seed_citation_denial src/relay/main.py` finds nothing (production never arms it).
    - Only `retrieved_ids` is mutated by the hook; no assignment to `app.state.registry` (grep the grow-step region).
    - Full free suite (`pytest -q`) still passes >= 256 tests with no behavior change (default off).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Thread the arming flag through run_case for the paid recovery case</name>
  <files>src/relay/evals.py</files>
  <read_first>
    - src/relay/evals.py:182-234 — run_case (the run_ticket call is at :198)
    - src/relay/evals.py:302-304 — the 0.8 threshold gate (do not touch)
    - 04-PATTERNS.md:212-217 — the self-analog threading note
    - 04-RESEARCH.md:306-307 — thread the arming flag into run_case for the paid case
  </read_first>
  <action>
    Add a keyword-only `seed_citation_denial: bool = False` param to `run_case` and pass it through to the `run_ticket(client, registry, ticket, ...)` call at evals.py:198 as `seed_citation_denial=seed_citation_denial`. Keep the default `False` so all 12 golden cases stay unarmed (Pitfall 4). Provide the wiring so the paid dispatch can arm exactly one report-only recovery case (D-08); do NOT arm it inside the default `run_evals` loop and do NOT change the 0.8 threshold gate (D-03/D-04). `CaseResult` already carries `citations`/`retrieval` — no dataclass change.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import inspect,relay.evals as e; p=inspect.signature(e.run_case).parameters['seed_citation_denial']; assert p.default is False" && grep -v '^#' src/relay/evals.py | grep -c 'seed_citation_denial=seed_citation_denial' && .venv/bin/python -m pytest tests/test_evals.py -q</automated>
  </verify>
  <done>run_case accepts and forwards seed_citation_denial (default False); the 12 golden cases stay unarmed; threshold gate untouched.</done>
  <acceptance_criteria>
    - `run_case` has keyword-only `seed_citation_denial` defaulting to `False`, forwarded to the run_ticket call at evals.py:198.
    - The default `run_evals` path never arms the flag (12 golden cases unchanged).
    - Mutation: if the flag is not forwarded (call left as `run_ticket(client, registry, ticket)`), the paid recovery case never fires a denial — no `guard="citation"` event in its report; Task 3's mechanism test also cannot force a denial via run_case.
    - The `pass_rate < threshold` gate at evals.py:302-304 is unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Free mechanism test — armed hook denies once, fake recovers</name>
  <files>tests/test_evals.py</files>
  <read_first>
    - tests/test_guardrails.py:441-473 — RecoveringFakeClient (reads retrieved_ids out of the denial)
    - tests/test_guardrails.py:476-490 — test_run_recovers_after_citation_denial (structure)
    - tests/test_guardrails.py:362-367 — keyword_baseline fixture
    - tests/test_guardrails.py:381-385 — _events_of; :214-226 — _seed_tickets/_reply_ticket_ids
    - 04-PATTERNS.md:181-189, 312-317 — the mechanism-test template + falsifiability property
  </read_first>
  <action>
    Add `test_seed_denial_hook_denies_then_fake_recovers` to `tests/test_evals.py`. Copy `RecoveringFakeClient` (it reads `last.get("retrieved_ids")` out of the denial and retries with a still-valid id — NOT scripted with the answer; this is the load-bearing property). Seed `TICKET`, pin `keyword_baseline` (no outbound call), and call `run_ticket(client, registry, TICKET, seed_citation_denial=True)`. The fake searches, cites the dropped id (triggering the seeded denial), then recovers.

    Assert: exactly one `guardrail` event with `guard == "citation"` fired, AND `events[-1]` is a `resolution` with `via == "send_reply"`, AND `_reply_ticket_ids(conn) == [TICKET["id"]]`. Add a `# mutation:` comment naming the falsifying change.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_evals.py -k seed_denial -q</automated>
  </verify>
  <done>The mechanism test arms the hook, asserts one citation guardrail + a terminal send_reply resolution + the correct reply row, and drives recovery off the denial payload; passes; names its mutation.</done>
  <acceptance_criteria>
    - Test arms `seed_citation_denial=True` and asserts: exactly one `guard="citation"` event, terminal `resolution via=send_reply`, and `_reply_ticket_ids(conn) == [TICKET id]`.
    - Recovery is driven off the denial payload (RecoveringFakeClient reads `retrieved_ids`), not a scripted answer.
    - Mutation A (04-PATTERNS.md:313-317): dropping `"retrieved_ids"` from the citation denial payload (agent.py:171) leaves the fake nothing valid to retry with -> recovery fails -> test fails.
    - Mutation B: making the hook a no-op (not dropping a real id) means no denial fires -> the `guard="citation"` count (== 1) assertion fails.
    - Test is free/deterministic (keyword mode, no outbound call).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval-only hook -> agent loop | the seeding param must never cross into production or the free suite's normal behavior |
| model citations -> reply (under a forced denial) | the guard must deny an unretrieved cite and remain recoverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-T | Tampering | seed_citation_denial hook (agent.py run_ticket) | mitigate | Keyword-only, default-off; only the per-run retrieved_ids set mutated (never the registry); main.py never sets it; full-suite floor >= 256 proves no behavior drift (Pitfall 4) |
| T-04-03-I | Information disclosure | citation guard denial payload | mitigate | Guard already names the reduced retrieved_ids so recovery stays possible; mechanism test fails if the payload stops naming valid ids |
| T-04-03-D | Denial of service | run ending "ended_without_action" on a real denial | accept | Denial is a retry instruction by construction; real-model recovery is a paid report-only finding (Pitfall 3), not a gate |
| T-04-03-SC | Tampering | package installs | accept | No installs (04-RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_evals.py -k seed_denial -q` green; full free suite `pytest -q` >= 256 passing; `ruff check src tests` clean.
- Isolation: `grep -q seed_citation_denial src/relay/main.py` finds nothing; no existing test changed behavior (default off).
- ci.yml and mcp_server.py byte-unchanged (`git diff --quiet .github/workflows/ci.yml src/relay/mcp_server.py`).
- Phase-gate (paid, manual, report-only — VALIDATION row 8): one `evals.yml` dispatch arms the seeded case and records whether a REAL model recovers (guard="citation" -> resolution via=send_reply) or ends `ended_without_action` — reported honestly either way (Open Question 3).
</verification>

<success_criteria>
- The D-08 hook exists as a keyword-only, default-off param on run_ticket; only the per-run set is mutated.
- run_case forwards the arming flag (default off); the 12 golden cases and the 0.8 gate are untouched.
- The free mechanism test proves denial + recovery with a payload-driven fake and a named mutation.
- Production (main.py) and the existing free suite are unaffected.
</success_criteria>

<output>
Create `.planning/phases/04-evaluation-coverage/04-03-SUMMARY.md` when done. Record the free mechanism-test result and, if a paid dispatch was run, whether the real model recovered from the seeded denial.
</output>
