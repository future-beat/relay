---
phase: 04-evaluation-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/retrieval.jsonl
  - src/relay/retrieval_eval.py
  - src/relay/evals.py
  - .github/workflows/evals.yml
  - tests/test_evals.py
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "Running the eval harness reports recall@1, recall@3, and MRR for the labeled retrieval set alongside the ticket results"
    - "The free test suite computes keyword-mode recall over the labeled set with zero Voyage calls"
    - "Every relevant id in the labeled set exists in kb/index.json (doc name or doc#slug)"
    - "The retrieval metric never gates CI — only the existing 12-case pass-rate threshold does"
  artifacts:
    - path: "evals/retrieval.jsonl"
      provides: "Labeled query -> relevant-chunk-id set (EVAL-01)"
      contains: "salesforce-integration"
      min_lines: 12
    - path: "src/relay/retrieval_eval.py"
      provides: "recall_at_k / mrr / load_labels pure functions over retrieve()"
      exports: ["recall_at_k", "mrr", "load_labels"]
    - path: "src/relay/evals.py"
      provides: "report-only retrieval_metrics field with keyword|semantic mode label"
      contains: "retrieval_metrics"
    - path: ".github/workflows/evals.yml"
      provides: "VOYAGE_API_KEY on the paid dispatch (D-11)"
      contains: "VOYAGE_API_KEY"
  key_links:
    - from: "src/relay/retrieval_eval.py"
      to: "src/relay/retrieval.py::retrieve"
      via: "calls the shipped retriever, never reimplements ranking"
      pattern: "retrieve\\("
    - from: "src/relay/evals.py::run_evals"
      to: "src/relay/retrieval_eval.py"
      via: "report-only retrieval_metrics field"
      pattern: "retrieval_metrics"
---

<objective>
Add the EVAL-01 retrieval eval set and metrics: a committed labeled query->relevant-id file, pure `recall_at_k`/`mrr` functions over the *shipped* `retrieve()`, a report-only `retrieval_metrics` field in the eval harness, and `VOYAGE_API_KEY` wired into the paid dispatch so semantic recall can be produced. Three free keyword-mode tests validate the set and the metrics.

Purpose: Prove retrieval quality is *measured*, not asserted — while honoring D-03 (report-only, soft floor `recall@3 > 0`, never a hard CI gate) and D-09/D-10 (recall@1 + MRR are the signal; semantic recall is paid).
Output: `evals/retrieval.jsonl`, `src/relay/retrieval_eval.py`, a report field + evals.yml secret, and three new tests riding the existing free `pytest -q` step (ci.yml UNCHANGED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evaluation-coverage/04-CONTEXT.md
@.planning/phases/04-evaluation-coverage/04-RESEARCH.md
@.planning/phases/04-evaluation-coverage/04-PATTERNS.md

<interfaces>
<!-- Verified contracts the executor needs. No codebase exploration required. -->

src/relay/retrieval.py:246 — the shipped retriever (call this, never reimplement):
    def retrieve(index, query, *, key=None, floor=..., max_results=3)
        -> tuple[list[dict], str, bool, str | None]
    # returns (results, mode, degraded, cause); each result has "doc", "id", "anchors", "text", "score"
    # No Voyage call when key is None -> keyword mode (this is what makes free CI possible)

src/relay/retrieval.py:438 — def anchors(doc) -> list[str]   # authority on what a doc licenses

src/relay/evals.py:65 — loader pattern to copy verbatim:
    def load_golden(path=GOLDEN_PATH) -> list[dict]:
        return [json.loads(line) for line in path.read_text().splitlines() if line.strip()]

src/relay/evals.py:255-264 — run_evals() return dict (the report); add keys beside pass_rate/mean_quality
src/relay/evals.py:267-284 — print_summary(); add a human-readable metrics line mirroring the footer
src/relay/evals.py:302-304 — the ONLY hard gate (pass_rate < threshold); DO NOT touch it

Accept-set union (copy from agent.py:318-323 / evals.py:131-132):
    got = {r["doc"] for r in results} | {r["id"] for r in results} | {a for r in results for a in r["anchors"]}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Labeled retrieval set + pure metric module</name>
  <files>evals/retrieval.jsonl, src/relay/retrieval_eval.py</files>
  <read_first>
    - 04-RESEARCH.md:247-262 — the verified ready-to-commit label set (ids checked against kb/index.json)
    - 04-PATTERNS.md:34-98 — eval-data analog (evals/golden.jsonl), loader analog, accept-set union
    - src/relay/retrieval.py:246-327 — retrieve() signature + return contract (the metric input)
    - src/relay/retrieval.py:438-450 — anchors() (what a doc licenses)
    - src/relay/evals.py:65-66 — load_golden() splitlines/json.loads loader to copy
    - evals/golden.jsonl — the per-line JSONL shape
  </read_first>
  <action>
    Create `evals/retrieval.jsonl` with the 12 rows verified in 04-RESEARCH.md:248-262 (per D-02: `{"id","query","relevant"}` per line; queries are model-style topical phrases, NOT ticket bodies; every non-empty `relevant` id is a real `kb/index.json` doc name or `doc#slug`; keep the `salesforce-integration` row with `relevant: []` as the negative/escalation case).

    Create `src/relay/retrieval_eval.py` (dedicated module per CLAUDE.md one-concern naming) with:
    - `load_labels(path=...)` copying the load_golden splitlines/json.loads pattern (evals.py:65-66).
    - `recall_at_k(index, labels, k, *, key=None)` and `mrr(index, labels, *, key=None)` as PURE functions that call the shipped `retrieve(index, row["query"], key=key, max_results=k)` — never reimplement ranking (Don't Hand-Roll). Build the accept-set per result as the doc/id/anchors union shown in the interfaces block. Per D-09 support recall@1, recall@3, MRR. EXCLUDE rows with `relevant: []` from recall/MRR (they are the escalation-signal negatives).
    Do not gate, print, or persist anything from this module — it is pure math.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; rows=[json.loads(l) for l in open('evals/retrieval.jsonl') if l.strip()]; assert len(rows)>=12, len(rows); assert any(r['relevant']==[] for r in rows); import relay.retrieval_eval as m; assert all(hasattr(m,n) for n in ('recall_at_k','mrr','load_labels'))"</automated>
  </verify>
  <done>evals/retrieval.jsonl has >=12 well-formed rows including the empty-relevant negative; retrieval_eval.py exports recall_at_k/mrr/load_labels and calls retrieve() (grep confirms).</done>
  <acceptance_criteria>
    - `evals/retrieval.jsonl` exists with >=12 lines and at least one `relevant: []` row.
    - `src/relay/retrieval_eval.py` exports `recall_at_k`, `mrr`, `load_labels`.
    - `grep -c 'retrieve(' src/relay/retrieval_eval.py` (filtered `grep -v '^#'`) >= 1 — the metric calls the shipped retriever, not a reimplementation.
    - Rows with `relevant: []` are excluded from recall/MRR arithmetic (verified in Task 3's test).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Report-only retrieval_metrics field + VOYAGE_API_KEY on the paid dispatch</name>
  <files>src/relay/evals.py, .github/workflows/evals.yml</files>
  <read_first>
    - src/relay/evals.py:255-284 — run_evals() return dict + print_summary() footer
    - src/relay/evals.py:302-304 — the ONLY hard gate (leave untouched)
    - 04-PATTERNS.md:193-217 — the self-analog report-field placement
    - .github/workflows/evals.yml:24-26 — the env block to extend
    - 04-RESEARCH.md:33-34 — D-10 (semantic is paid) / D-11 (add the secret)
  </read_first>
  <action>
    In `run_evals()` return dict (evals.py:255-264) add a report-only key `"retrieval_metrics"` = `{recall@1, recall@3, mrr, "mode": "semantic" if settings.voyage_api_key else "keyword"}`, computed via `retrieval_eval` over `load_labels()`, calling retrieve with `key=settings.voyage_api_key`. In `print_summary()` (evals.py:267-284) add one human-readable line mirroring the existing pass-rate footer, and label the `mode` so keyword-vs-semantic is never confused (Pitfall 2). Lead the printed line with recall@1 + MRR (D-09).

    Do NOT gate on these numbers — leave the `pass_rate < args.threshold` check at evals.py:302-304 exactly as-is (D-03).

    In `.github/workflows/evals.yml` add `VOYAGE_API_KEY: ${{ secrets.VOYAGE_API_KEY }}` to the `env:` block at line 24-26 (D-11). If the repo secret is absent the paid run reports keyword-mode recall — state this in the SUMMARY. Do NOT touch `.github/workflows/ci.yml` (frozen).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import relay.evals" && grep -v '^#' src/relay/evals.py | grep -c retrieval_metrics && grep -c 'VOYAGE_API_KEY' .github/workflows/evals.yml && git diff --quiet .github/workflows/ci.yml && echo CI_UNCHANGED</automated>
  </verify>
  <done>run_evals() returns a labeled retrieval_metrics field; print_summary prints recall@1/MRR/mode; evals.yml env has VOYAGE_API_KEY; ci.yml byte-unchanged; threshold gate line untouched.</done>
  <acceptance_criteria>
    - `run_evals()` return dict contains `retrieval_metrics` with a `mode` of `keyword` or `semantic`.
    - The `pass_rate < args.threshold` gate at evals.py:302-304 is unchanged (report-only, D-03).
    - `evals.yml` env block includes `VOYAGE_API_KEY: ${{ secrets.VOYAGE_API_KEY }}`.
    - `git diff --quiet .github/workflows/ci.yml` exits 0 (ci.yml frozen).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Free keyword-mode tests — labels well-formed, recall/MRR, soft floor</name>
  <files>tests/test_evals.py</files>
  <read_first>
    - tests/test_evals.py:13-22 — test_golden_dataset_is_well_formed (well-formedness analog)
    - tests/test_guardrails.py:362-367 — keyword_baseline fixture (pins settings.voyage_api_key=None)
    - tests/conftest.py:63-79 — _no_outbound_http autouse guard (raises on any real Voyage call)
    - 04-PATTERNS.md:165-179 — soft-floor + label analogs; keyword pinning requirement
    - 04-RESEARCH.md:117-137 — metric definitions
  </read_first>
  <action>
    Add three tests to `tests/test_evals.py`, ALL pinning `settings.voyage_api_key = None` (keyword mode) via monkeypatch so the autouse `_no_outbound_http` guard never raises (Pitfall 5) and the free suite bills nothing:
    1. `test_retrieval_labels_well_formed` — every non-empty `relevant` id in `evals/retrieval.jsonl` is a doc name or `doc#slug` present in `kb/index.json`; ids are unique; queries non-empty (copy the golden-dataset well-formedness shape).
    2. `test_recall_and_mrr_over_labeled_set` — `recall_at_k`/`mrr` return values in [0,1] on the labeled set in keyword mode; assert recall@1 == 1.0 for at least one query whose relevant doc keyword-matches (proves the metric actually consulted `retrieve()`).
    3. `test_soft_floor_recall3_positive` — assert `recall@3 > 0` only (D-03 wiring tripwire; NOT a hard threshold, NOT compared to a numeric floor above 0).
    Add a `# mutation:` comment to each test naming the change that makes it fail (below).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_evals.py -k "retrieval_labels or recall or soft_floor" -q</automated>
  </verify>
  <done>Three keyword-mode tests pass; each names its falsifying mutation; none makes an outbound call.</done>
  <acceptance_criteria>
    - All three tests pin keyword mode (`settings.voyage_api_key = None`) and pass under `pytest -k "retrieval_labels or recall or soft_floor"`.
    - Mutation (labels): changing a `relevant` id in retrieval.jsonl to a bogus id, OR removing the id-existence check, makes `test_retrieval_labels_well_formed` fail.
    - Mutation (recall/MRR): replacing the `retrieve()` call with a stub returning `[]` drops recall@1 to 0.0 and fails `test_recall_and_mrr_over_labeled_set`.
    - Mutation (soft floor): making `recall_at_k` return 0.0 (dead retrieval) fails `test_soft_floor_recall3_positive`.
    - Full free suite still collects and passes >= 256 tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI secrets -> workflow env | `VOYAGE_API_KEY` crosses into the paid job; must never be inlined |
| label data -> metric | untrusted-looking `relevant` ids could reference non-existent docs and manufacture fake recall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-I | Information disclosure | `.github/workflows/evals.yml` | mitigate | Reference `${{ secrets.VOYAGE_API_KEY }}` only; never echo or inline the key |
| T-04-01-T | Tampering | `evals/retrieval.jsonl` labels | mitigate | `test_retrieval_labels_well_formed` validates every id against `kb/index.json`; bogus ids fail CI |
| T-04-01-R | Repudiation | retrieval_metrics report | accept | Report-only, mode-labeled; no gating, no audit requirement (D-03) |
| T-04-01-SC | Tampering | package installs | accept | No installs — numpy/pytest already vendored (04-RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Free: `.venv/bin/python -m pytest tests/test_evals.py -q` green; full suite `pytest -q` >= 256 passing; `ruff check src tests` clean.
- ci.yml byte-unchanged (`git diff --quiet .github/workflows/ci.yml`).
- Phase-gate (paid, manual, report-only — VALIDATION row 4): a single `evals.yml` dispatch confirms the 12-case suite stays >= 0.8 and records semantic recall@1/MRR into the artifact. This plan does not gate on it.
</verification>

<success_criteria>
- EVAL-01 labeled set committed and validated against kb/index.json.
- recall@1/recall@3/MRR reported (report-only, mode-labeled); soft floor recall@3 > 0 asserted.
- VOYAGE_API_KEY wired into the paid dispatch; ci.yml untouched.
- Three free keyword-mode tests green, each with a named mutation.
</success_criteria>

<output>
Create `.planning/phases/04-evaluation-coverage/04-01-SUMMARY.md` when done. Record the observed keyword-mode recall@1/recall@3/MRR and whether the `VOYAGE_API_KEY` repo secret exists.
</output>
