---
phase: 06-dashboard-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/relay/db.py
  - src/relay/events.py
  - src/relay/config.py
  - src/relay/ratelimit.py
  - tests/test_dashboard.py
autonomous: true
requirements: [DASH-03, DASH-04]

must_haves:
  truths:
    - "A guarded, idempotent ALTER adds run_events.elapsed_ms and tickets.origin to a database that already exists; a second init_db on a populated DB does not raise and legacy rows keep NULL"
    - "Every run_events row written by RunRecorder carries elapsed_ms measured from a per-run monotonic origin, non-decreasing in seq order, on both the read-tool path and the write-tool (in-transaction) path"
    - "ratelimit.budget_snapshot(conn) is the ONE producer of today's spend/ceiling/remaining/resets_at, and enforce_daily_budget refuses from that same snapshot with its three asserted 503 detail keys unchanged"
    - "A ('run_detail', 'anon') rate-limit bucket and its settings attribute both exist, so the Wave-3 drill-down route cannot 500 on a _LIMIT_SETTINGS KeyError"
    - "New settings (run_detail_max_events, metrics_window_days, anon_run_detail_limit) are defaulted, so nothing new must be configured to deploy"
  artifacts:
    - path: "src/relay/db.py"
      provides: "_add_column_if_missing helper + three guarded ALTERs (runs.run_uid, run_events.elapsed_ms, tickets.origin)"
      contains: "_add_column_if_missing"
    - path: "src/relay/events.py"
      provides: "RunRecorder monotonic origin + elapsed_ms on the INSERT"
      contains: "elapsed_ms"
    - path: "src/relay/ratelimit.py"
      provides: "budget_snapshot() as the single arithmetic for gate and gauge (D-11); ('run_detail','anon') limit entry"
      exports: ["budget_snapshot"]
    - path: "src/relay/config.py"
      provides: "run_detail_max_events / metrics_window_days / anon_run_detail_limit defaults"
      contains: "run_detail_max_events"
    - path: "tests/test_dashboard.py"
      provides: "Phase 6 test module (created here): migration idempotency, elapsed_ms stamping, budget snapshot/gate agreement"
      exports: ["test_phase6_migrations_are_idempotent", "test_run_events_carry_elapsed_ms", "test_budget_snapshot_and_the_gate_cannot_disagree"]
  key_links:
    - from: "src/relay/db.py:init_db"
      to: "run_events.elapsed_ms / tickets.origin"
      via: "_add_column_if_missing(conn, table, column, decl) guarded by PRAGMA table_info"
      pattern: "_add_column_if_missing\\(conn, \"run_events\", \"elapsed_ms\""
    - from: "src/relay/events.py:RunRecorder._insert_event"
      to: "run_events.elapsed_ms"
      via: "int((time.monotonic() - self._t0) * 1000) in the INSERT tuple"
      pattern: "elapsed_ms"
    - from: "src/relay/ratelimit.py:enforce_daily_budget"
      to: "budget_snapshot"
      via: "the gate reads the same dict the /metrics gauge will read (D-11)"
      pattern: "budget_snapshot\\("
---

<objective>
Lay Phase 6's data foundations: the two guarded column migrations the drill-down and the demo exception need (`run_events.elapsed_ms`, `tickets.origin`), the `RunRecorder` change that stamps per-step millisecond timings, the single budget arithmetic (`budget_snapshot`) that D-11 requires the gauge and the gate to share, and the three defaulted settings the later waves read.

Purpose: DASH-03's "timings" has no source in today's schema — `run_events.created_at` is `datetime('now')`, whole seconds, and `RunRecorder`'s own docstring says that is why `seq` exists. D-02's full-fidelity exception has no server-side marker to read. D-11's gauge has no server-computed number to render. All three are data-layer gaps, and every later plan in this phase reads what this one writes.

Output: Changed `db.py`, `events.py`, `config.py`, `ratelimit.py`; new `tests/test_dashboard.py` seeded with the migration, timing and budget tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-experience/06-CONTEXT.md
@.planning/phases/06-dashboard-experience/06-RESEARCH.md
@.planning/phases/06-dashboard-experience/06-PATTERNS.md

<interfaces>
<!-- Contracts this plan modifies. Extracted from the codebase — use directly, no exploration. -->

db.py `init_db` (src/relay/db.py:268-285) already carries the exact idiom to generalise: it reads
`cols = {r["name"] for r in conn.execute("PRAGMA table_info(runs)").fetchall()}` and only then runs
`ALTER TABLE runs ADD COLUMN run_uid TEXT`. Its comment (L270-275) is the rationale to preserve.
`init_db` receives a `Database` (db.py:98-155), never a raw sqlite3.Connection: use
`conn.execute(sql).fetchall()`, never `conn.cursor()`.

db.py `SCHEMA` (src/relay/db.py:19-88): `tickets` is L27-35, `run_events` is L71-79. Neither gains a
column in the DDL — the precedent set by `runs.run_uid` is that the column exists ONLY via the ALTER,
so a fresh DB and the live Fly volume take the same code path and every test exercises the migration.

events.py `RunRecorder` (src/relay/events.py:388-425): `__init__(self, conn, *, run_uid, ticket_id)`
sets `self._seq = 0`; `_insert_event(self, type, data)` increments `_seq` and runs one INSERT with
`(run_uid, ticket_id, seq, type, json.dumps(data, default=str))`. `import time` is already at L31.
Two callers: `record()` (L427-436, its own transaction) and `execute_and_record()` (L438-480, inside
the write tool's transaction). Both go through `_insert_event`, so stamping there covers both.

ratelimit.py `spent_today` (src/relay/ratelimit.py:155-163) = `DAILY_SPEND_SQL` + `reserved_usd()`.
`enforce_daily_budget` (L204-230) raises 503 with detail keys `error` / `spent_usd` / `limit_usd` /
`resets_at` / `note`; `tests/test_ratelimit.py:226-229` asserts three of them by name.
`next_utc_midnight()` is L150-152. `_LIMIT_SETTINGS` (L54-71) maps `(bucket, tier)` to a settings
attribute NAME and a missing key is a hard `KeyError` inside `_limit_item` (L81-85) — i.e. a 500 on
the new route's first request, not a default.

config.py `Settings` (src/relay/config.py:18-151): each phase appends a commented block of defaulted
fields. `anon_events_limit` (L61-68) is the model for a rate-limit setting that carries its reasoning;
`events_retention_days` (L138-150) is the model for a windowing setting.

tests/conftest.py: `conn` (:memory:), `db` (file-backed tmp_path), `client` (TestClient + lifespan,
owner key on default headers, `demo_key="test-demo-key"`), autouse `_reset_limits` and
`_no_outbound_http`. `tests/helpers.py` holds `FakeClient`, `response`, `tool_use_block`, `text_block`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _add_column_if_missing + the elapsed_ms and origin migrations</name>
  <files>src/relay/db.py, tests/test_dashboard.py</files>
  <read_first>
    - src/relay/db.py:19-88 (SCHEMA — the tickets and run_events blocks that must stay unchanged) and :268-285 (init_db's existing guarded ALTER, comment included)
    - .planning/phases/06-dashboard-experience/06-RESEARCH.md:690-711 (Code Example: the helper, verbatim) and :647-648 (Pitfall 2 — why the column is NOT added to the DDL)
    - tests/test_run_events.py:61-100 (test_run_uid_migration_is_idempotent — the idiom to extend, mutation docstring included)
    - tests/conftest.py:33-41 (the file-backed `db` fixture; a :memory: DB is always fresh and would make this vacuous)
  </read_first>
  <behavior>
    - test_phase6_migrations_are_idempotent (unit, file-backed tmp_path DB): init_db once; INSERT a tickets row and a run_events row through raw SQL WITHOUT the new columns (simulating live-volume rows); call init_db a SECOND time — must not raise; then assert PRAGMA table_info(run_events) contains elapsed_ms, PRAGMA table_info(tickets) contains origin, PRAGMA table_info(runs) still contains run_uid, and the pre-existing rows read back with elapsed_ms IS NULL and origin IS NULL.
    - MUTATION that must turn it red, stated in the docstring: drop the PRAGMA guard inside _add_column_if_missing and ALTER unconditionally — the second init_db raises sqlite3.OperationalError "duplicate column name".
    - SECOND MUTATION that must turn it red: add `elapsed_ms INTEGER` to the run_events CREATE TABLE in SCHEMA and delete the ALTER — a fresh DB stays green, but this test's populated-DB path still shows the column missing after the second init_db only if the table pre-dates it; assert instead on the helper being the sole source by grepping that the SCHEMA blocks for tickets and run_events name neither `origin` nor `elapsed_ms` (a source assertion in the same test, commented as such).
  </behavior>
  <action>
    Add a module-level `def _add_column_if_missing(conn: Database, table: str, column: str, decl: str) -> None` to db.py directly above `init_db`, with the docstring from RESEARCH Code Example (D-13's reasoning, plus the note that WR-08's check-then-act race is deliberately deferred: single-machine, single-writer). Read the existing columns with `PRAGMA table_info({table})` and execute `ALTER TABLE {table} ADD COLUMN {column} {decl}` only when absent. Include the one-line comment RESEARCH asks for stating that `table`/`column` are module-local literals and never request-derived, so the f-string is not an injection site — a reviewer will look. Then replace the inline `run_uid` block in `init_db` with three calls: `_add_column_if_missing(conn, "runs", "run_uid", "TEXT")`, `_add_column_if_missing(conn, "run_events", "elapsed_ms", "INTEGER")`, `_add_column_if_missing(conn, "tickets", "origin", "TEXT")`. Move the existing D-13 comment onto the helper rather than deleting it, and leave a one-line pointer at the call site. Do NOT add either new column to the SCHEMA DDL and do not reorder the seed-count or `conn.commit()`. Add a comment on the `tickets.origin` call naming the values it carries ('demo' | 'owner' | NULL) and stating that NULL is legacy and means redacted — fail-closed per D-02. Create tests/test_dashboard.py with a module docstring stating what this file is (Phase 6's tests; test_run_events.py is Phase 5's and stays at 2127 lines) and the behaviour above.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard.py::test_phase6_migrations_are_idempotent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "_add_column_if_missing" src/relay/db.py` returns >= 4 (definition + three call sites).
    - Source: `grep -v '^#' src/relay/db.py | grep -c "ALTER TABLE"` returns exactly 1 — one ALTER, in the helper, never open-coded again.
    - Source: the `run_events` and `tickets` CREATE TABLE blocks in SCHEMA name neither `elapsed_ms` nor `origin`.
    - Behavior: the named test passes; its docstring names the unguarded-ALTER mutation.
    - Regression: `.venv/bin/python -m pytest tests/test_db.py tests/test_run_events.py -q` stays green.
  </acceptance_criteria>
  <done>One guarded, idempotent migration helper; three columns added through it; a populated DB survives a second init_db with legacy rows NULL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunRecorder stamps elapsed_ms from a per-run monotonic origin</name>
  <files>src/relay/events.py, tests/test_dashboard.py</files>
  <read_first>
    - src/relay/events.py:388-425 (RunRecorder docstring — it already explains why created_at cannot carry a timing) and :427-480 (both callers)
    - .planning/phases/06-dashboard-experience/06-RESEARCH.md:713-730 (Code Example: the exact stamping shape) and :644-645 (Pitfall 1)
    - tests/test_run_events.py:816-860 (test_a_run_persists_its_full_event_sequence — the run-driving idiom and the capture_frames fixture)
  </read_first>
  <behavior>
    - test_run_events_carry_elapsed_ms (integration, via the `client` + `capture_frames` fixtures with a scripted FakeClient run that includes one read tool and one write tool): after the run, SELECT seq, type, elapsed_ms FROM run_events ORDER BY seq — every row has a non-NULL integer elapsed_ms, the values are non-decreasing in seq order, the first row's value is small (< 5000), and BOTH a read-tool row (recorded via RunRecorder.record) and a write-tool row (recorded via execute_and_record, inside the tool's transaction) are stamped.
    - MUTATION that must turn it red, named in the docstring: drop `elapsed_ms` from the INSERT column list and its value from the tuple — every row reads NULL and the non-NULL assertion fails. A second, independent mutation: move `self._t0 = time.monotonic()` from `__init__` into `_insert_event` — every row then reads ~0 and the "the last row is greater than the first" assertion fails (script the run so at least one step is measurably later, e.g. by asserting strict monotonicity across a run with a `time.sleep(0.01)` injected via a fake tool, or by asserting `rows[-1]["elapsed_ms"] >= rows[0]["elapsed_ms"]` plus at least one non-zero value).
  </behavior>
  <action>
    In `RunRecorder.__init__`, capture `self._t0 = time.monotonic()` with the comment RESEARCH gives: the drill-down needs sub-second step timings and `created_at` is `datetime('now')` — whole seconds, which is also why `seq` exists. In `_insert_event`, add `elapsed_ms` to the INSERT column list and `int((time.monotonic() - self._t0) * 1000)` to the values tuple. Stamp in `_insert_event` and nowhere else, so both callers (`record` and `execute_and_record`) are covered by construction — and note in the docstring that for a write tool the row is inserted after `execute_bound` returns inside the tool's own transaction, so `elapsed_ms(tool_result) - elapsed_ms(tool_use)` is that tool's wall time, which is exactly what the Wave-2 drill-down projector will subtract. Do not touch `_seq`, the transaction structure, or `json.dumps(data, default=str)`. Append the behaviour above to tests/test_dashboard.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard.py::test_run_events_carry_elapsed_ms -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "elapsed_ms" src/relay/events.py` returns >= 2 (column list + value) and `grep -c "_t0" src/relay/events.py` returns >= 2.
    - Behavior: the named test passes and its docstring names both mutations.
    - Regression: `.venv/bin/python -m pytest tests/test_run_events.py -q` stays green — the atomicity and seq-ordering tests must be unaffected by the extra column.
  </acceptance_criteria>
  <done>Every persisted run event carries a millisecond offset from its run's start, on both write paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: budget_snapshot (one arithmetic, two consumers) + the run_detail limit bucket and settings</name>
  <files>src/relay/ratelimit.py, src/relay/config.py, tests/test_dashboard.py</files>
  <read_first>
    - src/relay/ratelimit.py:54-85 (_LIMIT_SETTINGS and _limit_item — the KeyError path), :150-163 (next_utc_midnight, spent_today), :204-230 (enforce_daily_budget, whose detail keys are asserted elsewhere)
    - src/relay/config.py:61-68 (anon_events_limit — the commented rate-limit setting to copy) and :138-150 (events_retention_days — the commented window setting to copy)
    - .planning/phases/06-dashboard-experience/06-RESEARCH.md:258-295 (Pattern 3, the target shape and why reserved_usd must stay in) and :650-651 (Pitfall 3 — the hard KeyError)
    - tests/test_ratelimit.py:200-240 (the existing 503 detail assertions this refactor must not move)
  </read_first>
  <behavior>
    - test_budget_snapshot_and_the_gate_cannot_disagree (unit, `conn` fixture + monkeypatched settings.max_daily_cost_usd): with spend below the ceiling, budget_snapshot returns spent_today_usd == round(spent_today(conn), 4), remaining_usd == ceiling - spent (floored at 0), exhausted False, and an ISO resets_at; then record enough cost to cross the ceiling and assert enforce_daily_budget raises HTTPException 503 whose detail["spent_usd"] and detail["limit_usd"] equal the snapshot's spent_today_usd and daily_ceiling_usd EXACTLY, and that snapshot["exhausted"] is True at the same instant. Include a reservation (reserve_run()) in one assertion so the snapshot is proven to include reserved_usd — the gate sees it, so the gauge must too.
    - MUTATION that must turn it red, named in the docstring: make budget_snapshot compute its own `SELECT SUM(cost_usd)` instead of calling spent_today — the reserved claim disappears from the snapshot while the gate still counts it, and the equality assertions fail. This is precisely the gauge-vs-gate disagreement D-11 forbids.
    - test_run_detail_limit_bucket_resolves (unit): `relay.ratelimit._limit_item("run_detail", "anon")` returns a parsed limit rather than raising KeyError, and it is NOT the same settings attribute as the events bucket.
  </behavior>
  <action>
    In ratelimit.py add `def budget_snapshot(conn: Database, *, now: float | None = None) -> dict` directly above `enforce_daily_budget`, returning `spent_today_usd` (round 4), `daily_ceiling_usd` (settings.max_daily_cost_usd), `remaining_usd` (round(max(ceiling - spent, 0.0), 4)), `exhausted` (spent >= ceiling) and `resets_at` (next_utc_midnight().isoformat()), computed from `spent_today(conn, now=now)`. Carry RESEARCH's docstring reasoning: it includes `reserved_usd` because that is what the gate compares, so a gauge summing only committed rows would read lower than the number that refuses the visitor's next run. Rewrite `enforce_daily_budget` to call it, return early when not exhausted, and build its 503 from the snapshot with the SAME detail key names (`error`, `spent_usd`, `limit_usd`, `resets_at`, `note`) and the same Retry-After arithmetic — `spent >= settings.max_daily_cost_usd` is byte-identical semantics, and tests/test_ratelimit.py asserts three of those keys. Add a `("run_detail", "anon"): "anon_run_detail_limit"` entry to `_LIMIT_SETTINGS` with a comment stating it is deliberately NOT the events bucket: a drill-down flood must not spend the live feed's reconnect allowance and silently break the feed for that visitor. In config.py append a commented "Dashboard experience (phase 6)" block with three defaulted fields: `anon_run_detail_limit: str = "120/minute"` (a public read that a page opens once per click, generous against real use and bounded against a scripted walk of the 30-day back catalogue), `run_detail_max_events: int = 400` (LIMIT on the per-run event read; ~10 rows/run today and max_agent_steps bounds a run, so this is headroom, not a truncation anyone will meet), and `metrics_window_days: int = 14` (how far back the daily cost/latency buckets reach; bounded so an ungated route polled every 5s per tab cannot grow with the life of the Fly volume). Match the existing "why this default" comment style. Append both behaviours to tests/test_dashboard.py.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard.py::test_budget_snapshot_and_the_gate_cannot_disagree tests/test_dashboard.py::test_run_detail_limit_bucket_resolves -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "budget_snapshot" src/relay/ratelimit.py` returns >= 2 (definition + the gate's call) and `grep -v '^#' src/relay/ratelimit.py | grep -c "spent_today("` shows the SUM is reached only through spent_today.
    - Source: `grep -c "run_detail" src/relay/ratelimit.py src/relay/config.py` returns >= 1 in each file.
    - Behavior: both named tests pass; the budget test's docstring names the parallel-SUM mutation.
    - Regression: `.venv/bin/python -m pytest tests/test_ratelimit.py -q` stays green — the 503 detail keys did not move.
  </acceptance_criteria>
  <done>One producer of the daily-budget numbers; the drill-down's rate-limit bucket exists before the route that reads it; three defaulted settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Fly volume ← init_db | Two ALTERs run against an existing prod DB holding real rows; a silent no-op ships a drill-down with no timings and a demo flag that is never set |
| public /metrics gauge ← budget arithmetic | The gauge will render a number a security control also computes; two producers means the displayed remaining budget can contradict the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | init_db ALTER TABLE (run_events, tickets) | mitigate | `_add_column_if_missing` PRAGMA guard; `test_phase6_migrations_are_idempotent` runs init_db twice on a populated file-backed DB and asserts legacy rows keep NULL |
| T-06-02 | Elevation of privilege | tickets.origin default | mitigate | The column is nullable with no default: legacy and non-demo rows are NULL, and Wave 3 reads NULL as "not demo" — fail-closed, matching `project()`'s unknown-type posture |
| T-06-03 | Information disclosure | budget_snapshot on a public route | accept | Every field is already public: `spent_usd`/`limit_usd`/`resets_at` ship in the 503 body any anonymous caller can trigger today |
| T-06-04 | Denial of service | new public route's limit bucket | mitigate | `("run_detail","anon")` is its own bucket with its own defaulted setting, so a drill-down flood cannot consume the live feed's allowance (Pitfall 3) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase — RESEARCH's Package Legitimacy Audit records the phase as install-free and a plan proposing one is a scope escape. No install task exists |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_dashboard.py -x -q` green (this plan's three test groups)
- `.venv/bin/python -m pytest -q` full suite >= 341 passing (no regression; test_db, test_run_events, test_ratelimit are the ones this plan can break)
- `.venv/bin/ruff check src tests` clean
- Manual, post-deploy (tracked in 06-VALIDATION.md): `sqlite3 /data/relay.db '.schema run_events'` shows `elapsed_ms` and `.schema tickets` shows `origin` on the live volume
</verification>

<success_criteria>
- `run_events.elapsed_ms` and `tickets.origin` exist on both a fresh DB and a populated one, added through one guarded helper
- Every persisted run event carries a monotonic millisecond offset, on the read-tool and write-tool paths alike
- `budget_snapshot` is the only producer of the daily-budget numbers and `enforce_daily_budget` reads it, with its 503 contract unchanged
- The `run_detail` limit bucket and three defaulted settings exist ahead of the routes that read them
- Full suite still >= 341 passing, ruff clean
</success_criteria>

<output>
Create `.planning/phases/06-dashboard-experience/06-01-SUMMARY.md` when done
</output>
